# 1-2_AI와 기계학습방법론

> **이 문서에 대하여 (튜터용 메타 정보)**
> SSAFY 강의 교재 「1-2_AI와 기계학습방법론」(총 225장) 전체를 개념 단위로 정리한 노트입니다.
> 원본은 4개 차시가 하나로 묶여 있습니다. `(p.NN)`은 원본 PDF/이미지의 **장 번호**입니다.
> 슬라이드에 `[HARD]` 표시가 있던 부분은 그대로 표기했습니다.
> 이 문서 밖의 지식은 수업 범위가 아닙니다. 설명은 여기 적힌 내용 안에서만 해 주세요.

**전체 구성**

| 차시 | 주제 | 범위 |
|---|---|---|
| 1차시 | 선형회귀(Linear Regression) | p.1-36 |
| 2차시 | 로지스틱회귀(Logistic Regression) | p.37-66 |
| 3차시 | 신경망모델(Neural Network) | p.67-149 |
| 4차시 | 신경망 적합(Fitting) — 손실·경사하강·역전파 | p.150-213 |
| 부록 | 참고 자료 / 실습 미리보기(MLP) | p.216-225 |

---

# 1차시 — AI & 기계학습 방법론 1: 선형회귀(Linear Regression)

## 강의 목차 (p.3)

1. 선형회귀: 입력과 출력의 선형 관계를 찾는 방법
2. 단순선형회귀: 하나의 입력 변수로 출력 예측하기
3. 다중선형회귀: 여러 입력 변수를 활용한 예측
4. 선형회귀 주의사항: 변수 상관(다중공선성), 상관과 인과 구분

## 학습 목표 (p.4)

- **선형회귀의 기본 아이디어를 설명할 수 있다.**
  - 입력과 출력 간의 직선적 관계를 그림과 수식으로 표현한다.
  - 직선을 잘 맞추기 위해 잔차(residual)와 최소제곱법이 어떻게 쓰이는지 직관적으로 설명한다.
- **회귀 계수의 의미를 해석할 수 있다.**
  - 계수가 크기/부호에 따라 어떤 의미를 갖는지 설명한다.
  - 결과의 불확실성을 표준오차, 신뢰구간의 기본 개념으로 설명한다.
- **모델의 성능을 평가할 수 있다.**
  - 잔차 표준 오차와 결정계수($R^2$)를 계산하고, 모델이 데이터를 얼마나 잘 설명하는지 비교한다.
- **다중 선형 회귀의 한계를 설명할 수 있다.**
  - 변수가 상관관계가 높을 때(다중공선성), 해석에 문제가 생기는 이유를 설명한다.
  - 상관관계와 인과관계는 다르다는 점을 사례로 구분한다.

## 0. 학습 시작 — 실제 응용 사례 (p.6)

- 광고비와 매출액의 상관 관계 분석
- 고객 소득·소비 패턴을 활용한 신용점수 산출

> **〈그림1〉 광고비와 매출액 상관관계 분석** — x축 광고비(만원, 200~1000), y축 매출액(만원, 500~2000+)의 산점도. 우상향 빨간 회귀선과 분홍색 95% 신뢰구간 띠. 범례는 "실제 데이터 / 회귀선: y = 1.46x + 518.23 / 95% 신뢰구간", 좌상단 주석은 $R^2 = 0.8844$, 상관계수 = 0.9404. 광고비가 늘수록 매출액이 거의 직선적으로 증가.
>
> **〈그림2〉 신용점수 산출 및 심사 자동화** — x축 신용등급 구간, 왼쪽 y축 고객 수(명) 막대, 오른쪽 y축 승인율(%) 꺾은선. 매우 낮음(300-400) 150명·5%, 낮음(400-500) 320명·15%, 보통(500-600) 580명·45%, 양호(600-700) 720명·75%, 우수(700-800) 450명·92%, 최우수(800-900) 180명·98%. 기준선(50%)이 점선. 신용점수가 높아질수록 승인율이 단조 증가.
>
> 출처: 삼성 SDS 홈페이지, https://www.shutterstock.com/ko/g/HobbitArt

## 1. 선형회귀(Linear Regression)란? (p.8-9)

- 입력 변수와 출력 변수 사이의 관계를 **직선 형태로 근사**하여 예측하는 통계적 방법
- 지도학습의 가장 **기초가 되는 접근** 중 하나
- 단순해 보이지만, 선형회귀는 **개념적으로도, 실무적으로도** 매우 유용

**광고데이터 예시 — 선형회귀를 통해 대답할 수 있는 질문들 (p.9)**

- 광고비와 매출 사이에 관계가 있는가?
- 그 관계의 강도는 어느 정도인가?
- 어떤 매체가 매출에 기여하는가?
- 미래 매출을 얼마나 정확히 예측할 수 있는가?
- 매체 간에 상호작용(시너지)가 있는가?

> **〈그림3〉 선형회귀 예시 그래프** — 세 개의 산점도가 나란히 있고 각각 x축이 TV(0~300), Radio(0~50), Newspaper(0~100), y축은 모두 Sales(5~25). 각 그림에 파란 회귀 직선. TV는 기울기가 뚜렷하고 점들이 직선 근처에 몰려 있는 반면 Newspaper는 점이 넓게 흩어져 있고 직선의 기울기가 완만함.

## 2-1. 단순선형회귀(simple linear regression) (p.11-12)

- 한 개의 설명변수($X$)와 하나의 반응변수($Y$) 사이의 선형(직선) 관계를 찾는 방법
- 목표: 데이터를 가장 잘 설명하는 직선을 찾아 예측($\hat{y}$)에 활용

**모형 가정**

$$Y = \beta_0 + \beta_1 X + \varepsilon$$

- $\beta_0$ : 절편 ($X = 0$일 때 $Y$ 값)
- $\beta_1$ : 기울기 ($X$가 1 단위 증가할 때 $Y$의 평균 증가량)
- $\varepsilon$ : 관측 오차
- hat 표기($\hat{y}$, $\hat{\beta}$)는 추정값을 의미
  - TV 광고비가 1억일 때 매출액 추정값은? $\hat{y} = \hat{\beta}_0 + 100\hat{\beta}_1$ → 약 1200만원

> **〈그림5〉 단순선형회귀 예시 그래프** — x축 TV(0~300, 단위: 1백만원), y축 Sales의 산점도와 파란 회귀 직선. p.12에서는 TV=100 지점에서 수직 점선을 올려 회귀선과 만나는 지점의 Sales 값(약 12)을 수평 점선으로 읽어내는 방식으로 예측값을 표시.

## 2-2. 최소제곱법(least squares) (p.13-14) `[HARD]`

- **실제 관측값과 예측값의 차이**(잔차, residual)를 제곱해 합한 값(RSS, **잔차제곱합**)을 최소화하는 방법
- 목표: 데이터를 가장 잘 설명하는 직선을 찾기 위해 계수 $\beta_0, \beta_1$을 추정

- 잔차(residual) 정의: $e_i = y_i - \hat{y}_i$ (예측값 $\hat{y}_i = \hat{\beta}_0 + \hat{\beta}_1 x_i$)
- RSS(잔차제곱합): $\mathrm{RSS} = e_1^2 + e_2^2 + \cdots + e_n^2$

$$\mathrm{RSS} = \sum_{i=1}^{n} e_i^2 = \sum_{i=1}^{n}(y_i - \hat{y}_i)^2 = \sum_{i=1}^{n}\left(y_i - \hat{\beta}_0 - \hat{\beta}_1 x_i\right)^2$$

> **〈그림6〉 실제 관측값과 예측값의 차이 – 잔차** — x축 X(2~10), y축 Y(7.5~25.0)의 산점도. 빨간 회귀선(범례 "회귀선: y = 1.98x + 5.88")과 파란 점("실제 관측값")이 있고, 각 점에서 회귀선까지 수직 점선이 그어져 있으며 "잔차"라는 주황색 화살표가 그 수직선을 가리킴.

**계수 추정 공식 (p.14)** — closed-form solution(공식으로 바로 계산할 수 있는 해)이 존재

$$\text{기울기}\quad \hat{\beta}_1 = \frac{\sum_{i=1}^{n}(x_i - \bar{x})(y_i - \bar{y})}{\sum_{i=1}^{n}(x_i - \bar{x})^2} \qquad \text{절편}\quad \hat{\beta}_0 = \bar{y} - \hat{\beta}_1 \bar{x}$$

- 참고: $\bar{y} = \frac{1}{n}\sum_i y_i$ , $\bar{x} = \frac{1}{n}\sum_i x_i$
- (안내) 1-2챕터 4차시에서 **경사 하강법**으로 선형회귀를 푸는 방법을 다룰 예정

## 2-3. 단순선형회귀: 광고 데이터 (p.15-18)

**사례 설명**

- 목표: TV 광고비($X$)와 제품 판매량($Y$)의 선형 관계 예측
- 단순선형회귀를 적용하여, 각 데이터에서 잔차제곱을 가장 작게 만드는 직선(최소제곱법)이 선택됨

**도형의 의미**

- **파란 직선**: 최소제곱법으로 계산한 회귀선
- **빨간 점**: 실제 관측 데이터
- **회색 세로선**(빨간 점에서 파란 선까지) = 잔차(residual)
- RSS를 최소화할 때, 최적의 $\hat{\beta}_0$, $\hat{\beta}_1$이 결정됨

**계수 해석 (p.16-17)**

- 절편(Intercept) = 7.03 → TV 광고비가 0이어도 기본적으로 평균 판매량은 7.03백만원
- TV 광고비 계수 = 0.0475 → TV 광고비를 1단위(1백만원) 늘리면 평균 매출이 약 0.0475×1백만원 = 4.72만원 증가

|  | Coefficient | Std. Error | t-statistic | p-value |
|---|---|---|---|---|
| Intercept | $\hat{\beta}_0 = 7.0325$ | 0.4578 | 15.36 | < 0.0001 |
| TV | $\hat{\beta}_1 = 0.0475$ | 0.0027 | 17.67 | < 0.0001 |

**t-statistic과 p-value, 유의성 검정 (p.18)**

- t-statistic $= \dfrac{\text{Coefficient}}{\text{Std. Error}}$
- t-statistic: 두 집단 간 평균 차이가 표본의 변동성(표준오차)에 비해 얼마나 큰지를 나타내는 값으로, 이 값이 클수록 평균 차이가 통계적으로 유의할 가능성이 커짐
- p-value: $\beta_1 = 0$ 일 때 관측값만큼(그 이상) 극단적일 확률
- 유의성 검정: 계수의 p-value < 0.0001 (매우 작음, ≪0.05) 이므로 통계적으로 매우 유의함 → TV 광고비와 매출 간 관계 존재

**모형 적합도 (p.18)**

| Quantity | Value |
|---|---|
| $R^2$ | 0.612 |

- $R^2$가 높을수록, 1에 가까울수록 좋음
- $R^2$ = 0.612 → 판매량 변동의 약 61%를 광고비로 설명 가능

## 3-1. 다중선형회귀(multiple linear regression) (p.20-22)

- 단순 선형 회귀: "TV 광고 → 매출" 한 가지 관계만 고려
- 다중 선형 회귀: "TV광고, Radio 광고비, 가격, 계절, 경쟁사" 등 복수 요인을 함께 고려하여 매출을 설명
- 독립 변수(설명 변수, Feature)가 여러 개 존재할 때 사용하는 회귀 분석 기법

$$Y = \beta_0 + \beta_1 X_1 + \beta_2 X_2 + \cdots + \beta_p X_p + \varepsilon$$

**각 변수의 의미 (p.22)**

- $Y$: 종속 변수 (예측 대상, 예: 매출)
- $X_1, X_2, X_3 \cdots, X_p$: $p$개의 독립 변수들 (예: 광고비, 가격, 계절 등)
- $\beta_0$: 절편
- $\beta_1, \beta_2, \cdots, \beta_p$: 각 독립 변수에 대한 회귀 계수(모수) — 변수의 영향력 크기와 방향을 나타냄
- $\varepsilon$: 관측 오차 (모델이 설명하지 못하는 부분, **오류/잔차가 아님!**)
- 해석: **다른 변수를 고정한 채** $X_j$가 1 단위 증가할 때 $Y$가 평균적으로 $\beta_j$만큼 변화
- 광고 데이터 예: $\text{sales} = \beta_0 + \beta_1 \mathrm{TV} + \beta_2 \mathrm{radio} + \beta_3 \mathrm{newspaper}$

> **비교 그림 (p.20)** — 왼쪽 〈그림7〉은 TV-Sales 2차원 산점도에 회귀 직선과 잔차 세로선("TV 광고만 고려"). 오른쪽 〈그림8〉은 축이 Sales(수직), TV, Radio인 3차원 그래프로, 파랑→초록 그라데이션의 평면(회귀 평면)과 빨간 관측점들이 평면 위아래에 수직선으로 연결("TV와 Radio까지 고려").

## 3-2. 다중선형회귀의 추정과 예측 (p.23)

- 여러 변수($x_{i1}, x_{i2}, \ldots, x_{ip}$)로 반응 변수 $Y$를 동시에 예측하는 모형
- 예측값: $\hat{y}_i = \hat{\beta}_0 + \hat{\beta}_1 x_{i1} + \hat{\beta}_2 x_{i2} + \cdots + \hat{\beta}_p x_{ip}$
- 추정 방법: 실제 값과 예측 값의 차이(잔차, $e_i = y_i - \hat{y}_i$)를 제곱해 합한 값(RSS)을 최소화
- RSS가 최소일 때 얻어지는 계수 $\hat{\beta}_0, \hat{\beta}_1, \ldots, \hat{\beta}_p$가 최적 추정치

$$\mathrm{RSS} = \sum_{i=1}^{n} e_i^2 = \sum_{i=1}^{n}(y_i - \hat{y}_i)^2 = \sum_{i=1}^{n}\left(y_i - \hat{\beta}_0 - \hat{\beta}_1 x_{i1} - \cdots - \hat{\beta}_p x_{ip}\right)^2$$

- 여러 입력 변수를 동시에 고려하여 데이터와 가장 가까운 평면(hyperplane)을 찾는 과정

## 3-3. 다중선형회귀 계수 추정 유도(행렬 표현) (p.24) `[HARD]`

$$\mathbf{y} = \begin{bmatrix} y_1 \\ y_2 \\ \vdots \\ y_n \end{bmatrix} = \begin{bmatrix} 1 & x_{11} & \ldots & x_{1p} \\ 1 & x_{21} & \ldots & x_{2p} \\ \vdots & \vdots & \vdots & \vdots \\ 1 & x_{n1} & \ldots & x_{np} \end{bmatrix} \begin{bmatrix} \beta_0 \\ \vdots \\ \beta_p \end{bmatrix} + \begin{bmatrix} \epsilon_1 \\ \epsilon_2 \\ \vdots \\ \epsilon_n \end{bmatrix} = \mathbf{X}\boldsymbol{\beta} + \boldsymbol{\epsilon}$$

**최소제곱법 목적: RSS 최소화**

$$\mathrm{RSS} = \left(\mathbf{y} - \mathbf{X}\hat{\boldsymbol{\beta}}\right)^{\top}\left(\mathbf{y} - \mathbf{X}\hat{\boldsymbol{\beta}}\right) = \mathbf{y}^{\top}\mathbf{y} - \hat{\boldsymbol{\beta}}^{\top}\mathbf{X}^{\top}\mathbf{y} - \mathbf{y}^{\top}\mathbf{X}\hat{\boldsymbol{\beta}} + \hat{\boldsymbol{\beta}}^{\top}\mathbf{X}^{\top}\mathbf{X}\boldsymbol{\beta}$$

미분 →

$$\frac{\partial \mathrm{RSS}}{\partial \hat{\boldsymbol{\beta}}} = -2\mathbf{X}^{\top}\mathbf{y} + 2\mathbf{X}^{\top}\mathbf{X}\hat{\boldsymbol{\beta}} = \mathbf{0}$$

- RSS가 가장 작아지는 계수를 찾기 위해, 함수의 기울기 = 0인 지점을 찾는 과정

**정규방정식 해 (closed-form 해)**

$$\hat{\boldsymbol{\beta}} = [\hat{\beta}_0, \hat{\beta}_1, \ldots, \hat{\beta}_p]^{\top} = \left(\mathbf{X}^{\top}\mathbf{X}\right)^{-1}\mathbf{X}^{\top}\mathbf{y}$$

## 3-4. 다중선형회귀 결과: 광고 데이터 (p.25-26)

- TV, 라디오 광고비는 매출 증가에 유의미한 관계를 가짐 (각 p-value가 매우 낮음 ≪ 0.05)
- 신문 광고비는 통계적으로 유의하지 않음 (p-value = 0.8599 > 0.05) → 매출과 관계가 거의 없음

|  | Coefficient | Std. Error | t-statistic | p-value |
|---|---|---|---|---|
| Intercept | 2.939 | 0.3119 | 9.42 | < 0.0001 |
| TV | $\hat{\beta}_1 =$ 0.046 | 0.0014 | 32.81 | < 0.0001 |
| radio | $\hat{\beta}_2 =$ 0.189 | 0.0086 | 21.89 | < 0.0001 |
| newspaper | $\hat{\beta}_3 =$ −0.001 | 0.0059 | −0.18 | 0.8599 |

**모형 적합도**

| Quantity | Value |
|---|---|
| $R^2$ | 0.897 |

- 결정계수 $R^2$ = 0.897 : 모델 설명력이 매우 높음 → 단순선형회귀 결과($R^2$=0.612)와 비교하였을 때 향상된 예측

> **〈그림8〉 광고비(TV, Radio)와 매출(Sales)의 다중선형회귀 결과** — 세로축 Sales, 나머지 두 축은 TV와 Radio인 3차원 그래프. 파랑(낮은 값)에서 초록(높은 값)으로 변하는 회귀 평면 위에 빨간 관측점들이 놓여 있고, 각 점에서 평면까지 검은 수직선(잔차)이 그어져 있음. 두 설명변수를 동시에 고려하면 회귀 결과가 직선이 아니라 평면이 됨을 보여줌.

## 4-1. 검증/테스트셋 데이터를 활용한 성능 평가 (p.28)

**훈련 데이터에서의 성능**

- 회귀식을 만들 때 최소제곱 해는 훈련 데이터만 보고 계산됨
- 학습에 사용된 훈련 데이터에서는(X와 Y의 평균적인 선형관계가 있다면) 적합(fitting)이 잘 되어 있을 것임
- 그러나 이것은 테스트 성능을 과소평가할 가능성이 높음

**테스트 성능 평가 필요**

- 선형회귀도 일반화 성능을 확인하려면 훈련에 사용되지 않은 새로운(테스트) 데이터에 적용해 봐야 함
- 변수가 많거나 고차항을 사용하면 **과적합(overfitting) 문제**가 여전히 발생할 수 있음
- 검증/교차검증을 통해서 적절한 적합을 찾을 수 있음

## 4-2. 다중선형회귀시 회귀계수 해석의 주의점 (p.29)

- 이상적 상황: 변수들이 연관(correlation)되지 않고 독립적일 때 → 계수 해석이 명확함
- 문제 상황: 변수들이 서로 연관되어 있다면 → 계수 추정이 불안정해지고 해석에 혼동이 발생할 수 있음
- 주의: 관찰 데이터의 상관관계로 인과 관계를 주장해서는 안 됨
  - 예로 들었던 광고 데이터는 자연스럽게 인과성이 있는 것처럼 보이지만, 다른 많은 데이터에서 선형 관계를 보인다고 하여 인과 관계가 있는 것이 아님
  - 예: "아이스크림 소비량"($X$) vs "상어에 물리는 사건"($Y$)

## 강의 정리 (1차시) (p.30-32)

**선형회귀 개요**
- 지도학습의 가장 기초적인 방법
- 입력과 출력의 선형 관계를 학습

**단순선형회귀**
- 모형: $Y = \beta_0 + \beta_1 X + \varepsilon$
- 의미: $\beta_0$ 절편, $\beta_1$ 기울기, $\varepsilon$ 측정오차
- 학습 방법: 최소제곱법 (RSS 최소화)
- 사례: TV 광고비와 매출 관계

**다중선형회귀**
- 모형: $Y = \beta_0 + \beta_1 X_1 + \beta_2 X_2 + \cdots + \beta_p X_p + \varepsilon$
- 해석: 다른 변수 고정 시, 특정 $X_j$가 1 증가 → $Y$는 평균적으로 $\beta_j$ 변화
- 사례: TV + Radio + Newspaper 광고비와 매출

**계수 추정과 예측**
- RSS 최소화를 통해 계수 추정
- 해: 최소제곱 ⇒ 정규방정식 사용
- 결과는 평면(hyperplane)으로 표현

**선형회귀 주의사항**
- 훈련 데이터 기반 계산 → 검증/테스트셋 필요
- 다중공선성 문제 발생 가능 (변수 간 높은 상관성) → 계수 해석 어려움, 분산 증가 → 불안정한 추정

## 확인문제 (1차시)

### 확인문제 1: 선형회귀 기초 (p.33-34)

1. 선형회귀의 기본 목적은 무엇인가?
2. 단순선형회귀 모형의 수식과 각 계수의 의미는?
3. 잔차(Residual)의 정의와 역할은?
4. 최소제곱법(Least Squares)의 핵심 아이디어는?

**정답 및 해설**
1. 입력 변수와 출력 변수 간의 선형 관계를 찾고, 이를 통해 예측 및 해석을 수행하는 것
2. 모형: $Y = \beta_0 + \beta_1 X + \varepsilon$, $\beta_0, \beta_1$은 절편과 기울기 모수, $\varepsilon$는 관측오차
3. 잔차 = 실제값 − 예측값. 모델이 설명하지 못한 부분으로, 모델 적합도를 판단하는 지표
4. 잔차제곱합(RSS)을 최소화하는 계수를 찾는 방법으로, 데이터와 회귀선 간의 차이를 최소화하는 원리

### 확인문제 2: 다중선형회귀 (p.35-36)

1. 다중선형회귀 모형의 수식을 쓰고, 각 항의 의미를 설명하시오.
2. 다중선형회귀에서 변수를 추가할 때 생길 수 있는 장점과 단점은 무엇인가?
3. 다중공선성이 발생하면 어떤 문제가 생기는가?
4. 다중선형회귀 모델의 성능을 평가하는 대표적인 지표 두 가지는 무엇인가?

**정답 및 해설**
1. $Y = \beta_0 + \beta_1 X_1 + \beta_2 X_2 + \cdots + \beta_p X_p + \varepsilon$, $Y$: 종속변수, $X$: 설명변수, $\beta_0$: 절편, $\beta_j$: 설명변수의 효과, $\varepsilon$: 오차항
2. 변수를 추가하면 더 많은 정보를 고려할 수 있어 설명력이 향상될 수 있다. 하지만 불필요한 변수를 넣으면 과적합 위험이 커지고 해석이 복잡해진다.
3. 다중공선성이 심하면 계수의 분산이 커져 추정이 불안정해지고, 해석이 어렵거나 예측력이 떨어진다.
4. 대표적인 지표로 결정계수 $R^2$과 잔차표준오차 RSE가 있다.

---

# 2차시 — 로지스틱회귀(Logistic Regression)

## 강의 목차 (p.38)

1. 분류(Classification) — 분류란? / 예시: 신용카드 연체(Default) / 분류 모델에서 선형 회귀의 한계
2. 로지스틱 회귀(Logistic Regression) — 로지스틱 회귀의 모형식 / MLE 활용 모수 추정 / 로지스틱 회귀 결과: 신용카드 연체 데이터

## 학습 목표 (p.39)

- 분류 모델에서 로지스틱 회귀를 사용하는 이유를 이해합니다.
- 로지스틱 회귀 모형을 이해합니다.
- 신용카드 연체의 예시를 통해 분류 모델을 적용해 봅니다.

## 1-1. 분류(Classification)란? (p.42-43)

**분류의 정의**

- 분류: 정해진 범주(카테고리) 중 하나로 지정하는 것
- 범주형 변수: 수치의 크고 작음이 아니라 유한한 범주(성별, 혈액형, 지역 등)로 표현하는 변수
  - 예: 눈동자 색 {black, brown, blue, green}, 이메일 종류 {spam, normal}

**분류 함수의 목표**

- 입력 ($X$, 특성 벡터): 눈동자 이미지, 이메일 내용/제목
- 출력 ($Y \in C$, 범주): 예, 눈동자 색 $C$ = {black, brown, blue, green}, 이메일 $C$ = {spam, normal} — 순서 없는 집합
- 분류 함수 $f(X)$를 학습하여 입력 $X$가 속할 범주(카테고리)를 예측
- 범주의 직접 예측보다 각 범주에 속할 확률 $P(Y = k \mid X)$를 추정하는 것이 더 유용할 때가 많음

## 1-2. 예시: 신용카드 연체(Default) (p.44-45)

**산점도 해석 (p.44)**

- 신용카드 사용량(Balance) - 소득(Income) 산점도에 연체(Default) 여부를 색상 및 부호로 구분
- 신용카드 사용량과 소득은 각각 독립변수, 연체 여부가 종속 변수
- 연체자(주황색 +)는 신용카드 사용량이 높은 구간에 집중적으로 분포
- 연체가 없는 사람(파란색 O)은 신용카드 사용량이 낮은 쪽에 주로 분포
- 소득은 연체 여부와 뚜렷한 상관이 보이지 않음

> **[산점도]** x축 Balance(0~2500), y축 Income(0~60000). 주황색 + 표시는 Balance가 큰 오른쪽 영역에 집중되고, 파란색 O 표시는 Balance가 작은 왼쪽 영역에 넓게 분포. Income 축 방향으로는 두 집단이 뚜렷이 갈리지 않음.

**그룹별 분포(Boxplot) 해석 (p.45)**

- 신용카드 사용량(Balance) vs 연체(Default)
  - 연체자의 신용카드 사용량이 연체하지 않은 사람보다 전반적으로 높음
  - 중앙값도 높고, 분포가 퍼져 있는 정도도 더 큼
- 소득(Income) vs 연체(Default)
  - 연체 여부에 따른 소득 차이는 거의 없음
  - 중앙값이 약간 다르지만 분포가 대부분 겹침

> **[Boxplot]** 왼쪽은 x축 Default(No, Yes), y축 Balance(0~2500)로 Yes 그룹 박스가 No 그룹보다 훨씬 위쪽(중앙값 약 1800 대 약 800)에 위치하고 폭도 넓음. 오른쪽은 x축 Default(No, Yes), y축 Income(0~60000)로 두 박스가 거의 같은 높이에서 크게 겹침.

## 1-3. 분류 문제에 선형회귀를 써도 될까? (p.46-47)

**선형회귀는 분류 문제에 사용하기에 부적절함: 이진 분류 문제**

- 선형회귀는 선형함수를 계산하는 문제로 예측 값이 (Y값 기준) 제한된 값을 갖지 못함
- 따라서 선형회귀는 **예측 확률이 0보다 작거나 1보다 크게 예측**될 수 있어 확률로 쓰기 **부적절함**
  - 예: 응급실 환자 진단 {0: 비응급, 1: 응급}
  - 선형회귀는 확률 범위(0~1)를 벗어나는 값을 내놓을 수 있어 문제 발생

**분류 문제에서 선형회귀의 대안 – 로지스틱 회귀(Logistic Regression) (p.47)**

- 시그모이드(Sigmoid) 함수를 활용해 0~1 범위 내 확률값 예측 보장
- 순서가 없는 범주를 확률로 직접 예측하는 적절한 분류 방법

> **[그림 p.46]** y축 "응급 여부"(비응급 0.0, 응급 1.0), x축 "입력 변수"(0~10). 파란 점들은 0.0 또는 1.0에만 놓여 있고, 빨간 직선은 x=0에서 −0.5, x=10에서 1.5까지 상승. 직선이 0 아래·1 위로 벗어난 두 삼각 영역이 붉게 칠해지고 "부적절"이라 표시.
>
> **[그림 p.47]** 같은 축에 초록색 S자 곡선이 0.0에서 시작해 x=5 부근에서 0.5를 지나 1.0으로 수렴. 곡선은 0~1 범위를 벗어나지 않음.

## 2-1. 로지스틱 회귀의 모형식 (p.49-50)

**시그모이드(Sigmoid) 함수** — 출력 범위가 0~1인, 기울어진 S자 형태의 곡선 함수

$$y = \frac{e^{z}}{1+e^{z}} = \frac{1}{1+e^{-z}}$$

(오일러 수 $e \approx 2.71828$)

- 모든 실수 $z$에 대한 $y$값의 범위는 $0 \le y \le 1$
  - $z \to +\infty$ 일 때, $y$는 1로 수렴
  - $z \to -\infty$ 일 때, $y$는 0으로 수렴

**로지스틱 회귀의 모형식 (p.50)**

시그모이드 함수의 $z$에 선형회귀 식 $z = \beta_0 + \beta_1 x$ 를 대입하면

$$y = \frac{e^{\beta_0 + \beta_1 x}}{1 + e^{\beta_0 + \beta_1 x}}$$

확률 표기 $p(X) = P(Y = 1 \mid X)$ 로 두면 (X가 주어졌을 때, 결과 Y가 1일 확률)

$$p(X; \boldsymbol{\beta}) = \frac{e^{\beta_0+\beta_1 x}}{1+e^{\beta_0+\beta_1 x}} = \frac{1}{1+e^{-(\beta_0+\beta_1 x)}}, \qquad \boldsymbol{\beta} = [\beta_0, \beta_1]^{\top}$$

> **[그림 p.50]** x축 Balance(0~2500), y축 Probability of Default(0.0~1.0). 주황색 점들이 0.0과 1.0 두 줄에 찍혀 있고, 파란 S자 곡선이 Balance가 커질수록 0에서 1로 완만히 상승.

## 2-2. 로지스틱 회귀 모형 vs 선형 모형 — 오즈와 로짓 (p.51-52) `[HARD]`

로지스틱 함수 $p(x;\boldsymbol{\beta})$는 선형 함수 $y = \beta_0 + \beta_1 x$ 를 내포하고 있다. 두 함수 간의 관계를 해석하기 위해 오즈(Odds)와 **로짓 변환**(logit)을 사용한다.

**오즈(Odds)** — 성공($y=1$) 확률이 실패($y=0$) 확률에 비해 몇 배 더 높은가

$$\text{odds} = \frac{p(y=1|x)}{p(y=0|x)} = \frac{p(y=1|x)}{1-p(y=1|x)} = \frac{\text{성공확률}}{\text{실패확률}}$$

**로짓 변환(logit) = Log odds** — 오즈에 log를 취한 함수 형태

$$\text{logit}(p) = \log(\text{odds}) = \log \frac{p(y=1|x)}{1-p(y=1|x)} = \log \frac{\text{성공확률}}{\text{실패확률}}$$

**로짓 변환 결과 (p.52)**

$$\text{logit}(p) = \log(odds) = \log\frac{p(y=1|x)}{1-p(y=1|x)} = \log \frac{p(x;\boldsymbol{\beta})}{1-p(x;\boldsymbol{\beta})} = \beta_0 + \beta_1 x$$

즉, **로지스틱 모형**식은 **선형** 모형식과 **시그모이드 함수의 결합**이며, **로짓 변환** 시 **선형** 회귀 모형식으로 표현이 가능하다.

| 로지스틱 모형 = 선형 모형 + 시그모이드 함수 | 로지스틱 모형의 로짓 변환 = 선형 모형 |
|---|---|
| $p(x;\boldsymbol{\beta}) = \dfrac{e^{\beta_0+\beta_1 x}}{1+e^{\beta_0+\beta_1 x}}$ | $\log \dfrac{p(x;\boldsymbol{\beta})}{1-p(x;\boldsymbol{\beta})} = \beta_0 + \beta_1 x$ |

## 2-3. MLE 활용 모수 추정 (p.53-56) `[HARD]`

**우도(Likelihood) (p.53)**

- 선형회귀에서 "현재 함수가 데이터와 오차가 작은지"를 평가하기 위해 평균 제곱 오차(MSE)를 지표로 삼았듯, 확률을 계산하는 함수를 평가하기 위해선 우도(Likelihood)를 지표로 삼는다.
- 우도란 "**현재 확률 함수가 데이터를 얼마나 잘 설명하는지**"를 나타낸 지표이다. 그러므로 모델의 학습은 우도 값을 최대화하는 것이 목표이며 이를 **Maximum Likelihood Estimation (MLE)**이라 한다.

**우도 최대화 (p.54, p.56)**

$$\underset{\boldsymbol{\beta}}{\text{maximize}}\ \mathcal{L}(\boldsymbol{\beta}) = \prod_{i:y_i=1} p(x_i;\boldsymbol{\beta}) \prod_{i:y_i=0} \left(1 - p(x_j;\boldsymbol{\beta})\right)$$

참고: $p(x_i;\beta) = \dfrac{e^{\beta_0+\beta_1 x_i}}{1+e^{\beta_0+\beta_1 x_i}}$

- 곱으로 이뤄진 함수는 $\beta$에 대해 미분이 어렵기 때문에 **양 변에 log를 취해 곱셈을 더하기로 변환**시킨 뒤 **log-likelihood**를 만들어 최대화한다.

$$\underset{\boldsymbol{\beta}}{\text{maximize}}\ \log \mathcal{L}(\boldsymbol{\beta}) = \sum_{i=1}^{n}\left[y_i \log p(x_i;\beta) + (1-y_i)\log(1 - p(x_i;\beta))\right]$$

- (p.56) $\log \mathcal{L}(\beta)$를 미분하여 **도함수 = 0**에 근접하도록 수치적(반복) 최적화를 통해 $\beta$들을 찾아 나간다.

**log 변환의 정당성 (p.55)**

- log 함수는 단조(monotone) 증가 함수임
- 따라서 log-likelihood를 최대화하는 $\boldsymbol{\beta}$와 likelihood를 최대화하는 $\boldsymbol{\beta}$는 같다!

> **[그림 p.55]** 위 그래프는 세로축 $\mathcal{L}(\boldsymbol{\beta})$(0.00~0.15), 가로축 $\boldsymbol{\beta}$(0.0~1.0)로 종 모양 곡선이 $\beta \approx 0.4$에서 최대. 아래 그래프는 세로축 $\log \mathcal{L}(\boldsymbol{\beta})$(−40~−10)로 완만한 볼록 곡선이 역시 $\beta \approx 0.4$에서 최대. 두 최대점이 같은 위치임을 빨간 점선과 $\widehat{\boldsymbol{\beta}}$ 표시로 연결.

## 2-4. 로지스틱 회귀 결과: 신용카드 연체 데이터 (p.57-59)

**단변수 모형 (p.57)**

|  | Coefficient | Std. error | $z$-statistic | $p$-value |
|---|---|---|---|---|
| Intercept | −10.6513 | 0.3612 | −29.5 | <0.0001 |
| balance | 0.0055 | 0.0002 | 24.9 | <0.0001 |

- 추정 결과 $\hat{\beta}_1$ = 0.0055 — 신용카드 사용량(Balance)이 1 단위 증가할 때 연체(Default)의 로짓(log-odds)이 0.0055 증가

$$\log \frac{p(X;\hat{\beta})}{1-p(X;\hat{\beta})} = -10.6513 + 0.0055 \cdot X$$

**예측 확률 계산 예시 (p.58)**

$X = 1000$일 때

$$p(X;\hat{\beta}) = \frac{e^{-10.6513+0.0055\cdot 1000}}{1+e^{-10.6513+0.0055\cdot 1000}} = 0.006$$

$X = 2000$일 때

$$p(X;\hat{\beta}) = \frac{e^{-10.6513+0.0055\cdot 2000}}{1+e^{-10.6513+0.0055\cdot 2000}} = 0.586$$

- 입력 값은 **2배 증가** → 예측 확률값은 **97배 증가함**

**다변수 모형 (p.59)**

신용카드 사용량 이외에도 다양한 입력(소득, 학생 여부 등)을 추가하여 여러 변수 $X_1, \dots, X_p$를 함께 모형에 사용하면 회귀 계수를 통해 연체할 확률을 계산할 수 있음.

$$\log \frac{p(X;\beta)}{1-p(X;\beta)} = \beta_0 + \beta_1 X_1 + \cdots + \beta_p X_p$$

$$p(X;\beta) = \frac{e^{\beta_0+\beta_1 X_1 + \cdots + \beta_p X_p}}{1+e^{\beta_0+\beta_1 X_1 + \cdots + \beta_p X_p}}$$

|  | Coefficient | Std. error | $z$-statistic | $p$-value |
|---|---|---|---|---|
| Intercept | −10.8690 | 0.4923 | −22.08 | <0.0001 |
| balance | 0.0057 | 0.0002 | 24.74 | <0.0001 |
| income | 0.0030 | 0.0082 | 0.37 | 0.7115 |
| student[Yes] | −0.6468 | 0.2362 | −2.74 | 0.0062 |

## 강의 정리 (2차시) (p.60)

**분류 문제란?**
- 분류: 정해진 범주(카테고리) 중 하나로 지정하는 것
- 분류 모델의 목표: 분류 함수 $f(X)$를 학습하여 입력 $X$가 속할 범주(또는 그 확률)를 예측

**분류 문제에서 선형회귀 모델 적용의 한계**
- 이진 분류 문제: 선형회귀는 예측 값이 확률 범위(0~1)를 벗어나 부적합
- 다중 분류 문제: 정수로 코딩 시 인위적 순서를 암시하기에 부적합

**로지스틱 회귀는 어떻게 작동할까?**
- 로지스틱 함수 모형식은 선형 회귀 모형식과 sigmoid 함수의 결합
- 우도: "확률함수가 데이터를 얼마나 잘 설명하는지"를 나타낸 지표
- 최대 우도 추정(MLE): 우도가 최대가 되는 모수를 찾는 과정

## 확인문제 (2차시)

### 확인문제 1: 다음 중 틀린 설명은? (p.61-62)

A. 다중범주 문제에는 선형회귀보다 (Multiclass) Logistic Regression 또는 다른 분류 모델이 적절하다.
B. 선형 회귀 예측 값을 확률로 해석하면 0미만 또는 1초과가 나올 수 있어 확률로서 타당하지 않다.
C. 기상 상태 예측 모델을 만들 때, $Y \in \{1: 맑음, 2: 눈, 3: 비\}$ 와 같이 정수형 코딩을 진행해야만 적절한 예측이 가능하다.
D. 범주형(질적) 변수는 순서가 없는 집합에서 값을 가진다.

**정답: C**
해설: 범주형 변수를 정수 코딩했을 때, 인위적 순서를 암시하고 1↔2와 2↔3의 거리 동일성까지 암묵적으로 가정하게 됨.

### 확인문제 2: 다음 중 로지스틱 함수의 출력으로 옳은 것은? (p.63-64)

A. $[0, 1]$  B. $(0, 1)$  C. $(-\infty, +\infty)$  D. $\{0, 1\}$

**정답: B**
해설: 로지스틱 회귀의 출력은 어떤 $X, \beta$에 대해서도 $p(X;\beta) \in (0,1)$가 보장됩니다. A가 답이 아닌 이유는 끝점 0과 1을 포함하기 때문입니다. 로지스틱 회귀의 출력 중 0과 1은 극한으로만 접근합니다.

### 확인문제 3: 다음 문장이 옳은가? (O / X) (p.65-66)

> 로지스틱 회귀에서 "현재 확률 함수가 데이터를 얼마나 잘 설명하는지"를 나타낸 지표로 사용하는 것은 평균 제곱 오차(MSE)이다.

**정답: X**
해설: 로지스틱 회귀에서 모수(가중치)를 추정할 때 사용하는 표준 지표는 평균제곱오차(MSE)가 아니라, 우도(likelihood)를 최대화하는 방법(MLE)이다.

---

# 3차시 — AI & 기계학습 방법론 3: 신경망모델(Neural Network)

## 강의 목차 (p.68)

1. Shallow 네트워크 — 구조 및 활성화함수 / 조각별 선형(piecewise linear) 표현 / 표현력·보편적 근사 정리
2. Deep Network — 네트워크 합성과 층별 출력, "접기(folding)" 직관 / Shallow vs Deep 비교 / 행렬·벡터 수식화와 도식

## 학습 목표 (p.69)

- 신경망이란 무엇인지를 알아보고, 기본 구성요소를 설명한다.
- Hidden unit의 역할(비선형성 도입·특징 변환)을 이해한다.
- Shallow 네트워크와 Deep 네트워크의 차이(구조, 표현력, 효율성)를 구분한다.

## 1. 모수적 함수로서의 선형 모델 (p.73-74)

**손실함수 (p.73)**

$$L[\boldsymbol{\phi}] = \sum_{i=1}^{I} (\mathrm{f}[x_i, \boldsymbol{\phi}] - y_i)^2 = \sum_{i=1}^{I} (\phi_0 + \phi_1 x_i - y_i)^2$$

→ **"최소제곱 손실함수"**

> **[그림 p.73]** 가로축 Input $x$ (0.0~2.0), 세로축 Output $y$ (0.0~2.0). 주황색 점들(데이터)과 $\phi_0=0.4,\ \phi_1=0.2$인 직선이 그려져 있고, 각 데이터점에서 직선까지 점선으로 수직 거리가 표시됨. 상단에 `Loss, L = 7.11`. 직선이 데이터를 잘 따라가지 못해 손실이 큰 상태.

**1D 선형모델 학습 (p.74)** — 순차적으로 모수(parameter)를 조정하면서 찾아내는 선형회귀

> **[그림 a]** 가로축 Intercept $\phi_0$ (0.0~2.0), 세로축 Slope $\phi_1$ (−1.0~1.0), 제목 `Loss, L[φ]`. 손실의 등고선 위에 0→1→2→3→4로 번호가 매겨진 점들이 화살표로 이어져 손실이 낮은 영역으로 이동.
> **[그림 b]** 가로축 Input $x$, 세로축 Output $y$. 데이터점과 함께 0~4로 번호 붙은 직선들이 점점 데이터에 잘 맞도록 회전·이동하며, 4번 직선이 데이터를 가장 잘 적합. a)의 각 단계가 b)의 각 직선에 대응.

## 2. Shallow 네트워크

### 2-1. Shallow 네트워크: 정의·장점·사용 예시 (p.76)

**정의**: 입력층(Input) - **은닉층(Hidden Layer) 1개** - 출력층(Output)으로 구성된, 1개의 은닉층만 가진 신경망 구조

**장점**
- 복잡한 비선형 패턴까지 학습 가능 — 단순 선형 모델 대비 표현력 향상
- 구조가 간단해 학습 속도가 빠르고 구현이 쉬움
- 데이터가 충분하지 않아도 과적합 위험이 비교적 낮음

**사용 예시**
- 간단한 분류(Classification) 및 회귀(Regression) 문제
- XOR 같은 비선형 결정 boundary가 필요한 문제

### 2-1. Shallow 네트워크 vs 1D 선형회귀 (p.77-78)

단순(1D) 선형회귀:

$$y = \mathrm{f}[x, \boldsymbol{\phi}] = \phi_0 + \phi_1 x$$

- $\phi_0, \phi_1$이 모수(parameter)

Shallow 네트워크의 예:

$$y = \mathrm{f}[x, \boldsymbol{\phi}] = \phi_0 + \phi_1 \mathrm{a}[\theta_{10} + \theta_{11}x] + \phi_2 \mathrm{a}[\theta_{20} + \theta_{21}x] + \phi_3 \mathrm{a}[\theta_{30} + \theta_{31}x]$$

- p.78에서는 $\phi_0, \phi_1, \phi_2, \phi_3, \theta_{10}, \theta_{11}, \theta_{20}, \theta_{21}, \theta_{30}, \theta_{31}$ 전부가 모수임을 표시.

### 2-2. 활성화 함수 (activation function) (p.79)

식에서 $\mathrm{a}[\cdot]$ 부분이 **활성화 함수**

$$\mathrm{a}[z] = \mathrm{ReLU}[z] = \begin{cases} 0 & z < 0 \\ z & z \ge 0 \end{cases}$$

**Rectified Linear Unit** (활성화 함수의 대표적 예)

> **[그림]** 가로축 $z$ (−5.0~5.0), 세로축 ReLU$[z]$. $z<0$에서 값이 0으로 평평하다가 $z=0$에서 꺾여 기울기 1로 상승하는 꺾은선.

### 2-3. 모수(parameter) (p.80)

위 네트워크 모델은 10개의 모수를 갖고 있음

$$\boldsymbol{\phi} = \{\phi_0, \phi_1, \phi_2, \phi_3, \theta_{10}, \theta_{11}, \theta_{20}, \theta_{21}, \theta_{30}, \theta_{31}\}$$

- 모수($\boldsymbol{\phi}$)가 정해지면 특정 함수($\mathrm{f}[x, \boldsymbol{\phi}]$)가 결정된다.
- 모수가 주어지면 추론(식을 계산, 예측)할 수 있다.
- 훈련데이터 $\{(x_i, y_i)\}_{i=1}^{I}$ 가 주어지면 **손실함수**(예: least squares)를 정의하고 손실을 최소화하도록 모수를 조정한다.

### 2-4. piecewise linear 함수 (p.81)

- 입력 구간을 나눠 **조각별 선형** 함수를 만듦
- 다른 모수($\boldsymbol{\phi}$)에 따라서 달라지는 함수들의 예 a), b), c)

> **[그림 a~c]** 세 그래프 모두 가로축 Input $x$ (0.0~2.0), 세로축 Output $y$ (−1.0~1.0). 각각 꺾인 점이 여러 개인 조각별 선형 곡선이며 모양이 서로 다름 — a) 내려갔다 다시 올라오는 형태, b) 평평하다가 올라간 뒤 급격히 내려가는 형태, c) 올라갔다가 길게 내려온 뒤 평평해지는 형태.

### 2-5. Hidden Units (p.82)

두 부분으로 나눠 생각:

$$y = \phi_0 + \phi_1 h_1 + \phi_2 h_2 + \phi_3 h_3$$

**Hidden units**

$$h_1 = \mathrm{a}[\theta_{10} + \theta_{11}x], \quad h_2 = \mathrm{a}[\theta_{20} + \theta_{21}x], \quad h_3 = \mathrm{a}[\theta_{30} + \theta_{31}x]$$

### 2-6. 각 단계별 계산 (p.83-86)

계산 과정을 세 단계(**선형 변환** → **활성화 함수 적용** → **활성화 후 $\phi_j$배 적용**) → 최종 합으로 나눠 보임. 모든 그래프의 가로축은 Input $x$ (0.0~2.0), 세로축은 Output (−1.0~1.0).

**선형 변환 (a·b·c)**
- a) $\theta_{10} + \theta_{11}x$ : 음수에서 시작해 오른쪽 위로 올라가는 직선(주황)
- b) $\theta_{20} + \theta_{21}x$ : 더 가파르게 올라가는 직선(연두), $x \approx 1.0$에서 0을 지남
- c) $\theta_{30} + \theta_{31}x$ : 왼쪽 위에서 오른쪽 아래로 내려가는 직선(검정), $x \approx 1.5$에서 0을 지남

**활성화 함수 적용 (d·e·f)**
- d) $h_1$ : $x \approx 0.5$까지 0으로 평평하다가 이후 상승
- e) $h_2$ : $x \approx 1.0$까지 0, 이후 가파르게 상승
- f) $h_3$ : 왼쪽 위에서 내려오다 $x \approx 1.5$부터 0으로 평평

**활성화 후 $\phi_j$배 적용 (g·h·i)**
- g) $\phi_1 h_1$ : $x \approx 0.5$까지 0, 이후 아래로 내려감(부호가 뒤집힘)
- h) $\phi_2 h_2$ : $x \approx 1.0$까지 0, 이후 더 가파르게 위로 상승
- i) $\phi_3 h_3$ : 위에서 내려오다 $x \approx 1.5$부터 0으로 평평

**g), h), i) 함수의 합 (j)**
- j) $\phi_0 + \phi_1 h_1 + \phi_2 h_2 + \phi_3 h_3$ : 내려갔다가 $x \approx 1.0$ 부근에서 최저가 된 후 다시 올라오는 조각별 선형 곡선. 개별 hidden unit 출력들의 가중합이 최종 piecewise linear 함수를 만든다는 것을 보여줌.

### 2-7. 네트워크 도식화 (p.87-88)

> **[그림 p.87]** 왼쪽에 입력 노드 $x$, 가운데에 은닉 노드 $h_1, h_2, h_3$(각 노드 안에 ReLU 모양의 꺾은선 표시), 오른쪽에 출력 노드 $y$. 추가로 상수 노드 "1"이 두 개 있어, 하나는 $\theta_{10}, \theta_{20}, \theta_{30}$ 가중치로 각 은닉 노드에 연결되고, 다른 하나는 $\phi_0$ 가중치로 $y$에 연결됨 — 즉 절편(bias)을 상수 1 노드로 표현.
> **[그림 p.88]** 같은 구조에서 상수 1 노드와 가중치 라벨을 생략한 **단순한 네트워크 표현** — $x$ → $h_1, h_2, h_3$ → $y$.

- 각 파라미터는 (출발 노드의 값)×(가중치)를 출력 노드에 더한다.

## 3. Shallow 네트워크의 표현력

### 3-1. 더 많은 Hidden Unit (p.90)

**3개의 hidden units:**

$$h_1 = \mathrm{a}[\theta_{10} + \theta_{11}x], \quad h_2 = \mathrm{a}[\theta_{20} + \theta_{21}x], \quad h_3 = \mathrm{a}[\theta_{30} + \theta_{31}x]$$
$$y = \phi_0 + \phi_1 h_1 + \phi_2 h_2 + \phi_3 h_3$$

**(일반적인) D개의 hidden units:**

$$h_d = \mathrm{a}[\theta_{d0} + \theta_{d1}x], \qquad y = \phi_0 + \sum_{d=1}^{D} \phi_d h_d$$

### 3-2~3-3. 보편적 근사 정리(Universal approximation theorem) (p.91-92)

충분히 많은 Hidden Unit이 있다면, 임의의 1차원 함수를 **원하는 정확도**로 근사할 수 있다.

> 정리: "**Hidden unit을 충분히 많이** 갖는다면, 얕은 신경망은 **임의의 연속함수를 임의의 정밀도로 근사**할 수 있음"

> **[그림]** 동일한 검은 점선 목표 함수(Input $x$: 0.0~2.0, Output $y$: −1.0~1.0)를 세 가지 조각선형 근사가 덮는 3개 패널. a) "5 linear regions" 굵은 민트색 꺾은선이 목표 곡선의 대략적 형태만 따라가며 봉우리·골에서 크게 벗어남. b) "10 linear regions" 주황색 꺾은선이 목표 곡선을 상당히 잘 따라감. c) "20 linear regions" 짙은 청회색 꺾은선이 검은 점선과 거의 완전히 겹침.

- 요지: linear region 수(= Hidden Unit 수)가 늘수록 근사 오차가 줄어든다.

## 4. 입력·출력 개수의 확장

### 4-1. 2개 출력의 네트워크 (p.94)

1 input, 4 hidden units, 2 outputs

$$h_1 = \mathrm{a}[\theta_{10} + \theta_{11}x],\quad h_2 = \mathrm{a}[\theta_{20} + \theta_{21}x],\quad h_3 = \mathrm{a}[\theta_{30} + \theta_{31}x],\quad h_4 = \mathrm{a}[\theta_{40} + \theta_{41}x]$$

$$y_1 = \phi_{10} + \phi_{11}h_1 + \phi_{12}h_2 + \phi_{13}h_3 + \phi_{14}h_4$$
$$y_2 = \phi_{20} + \phi_{21}h_1 + \phi_{22}h_2 + \phi_{23}h_3 + \phi_{24}h_4$$

> **[그림]** 가로축 Input $x$ (0.0~2.0), 세로축 Output $y$ (−1.0~1.0). 청회색 선 $y_1$과 주황색 선 $y_2$가 그려져 있고, 세로 점선으로 표시된 꺾임 지점(joint) 위치가 두 출력에서 동일. 두 출력은 같은 위치에서 꺾이지만 서로 다른 조각선형 함수를 만든다.

### 4-2. 2개 입력의 네트워크 (p.95)

2 input, 3 hidden units, 1 output

$$h_1 = \mathrm{a}[\theta_{10} + \theta_{11}x_1 + \theta_{12}x_2],\quad h_2 = \mathrm{a}[\theta_{20} + \theta_{21}x_1 + \theta_{22}x_2],\quad h_3 = \mathrm{a}[\theta_{30} + \theta_{31}x_1 + \theta_{32}x_2]$$

$$y = \phi_0 + \phi_1 h_1 + \phi_2 h_2 + \phi_3 h_3$$

### 4-3. 2개 입력 네트워크: 단계별 계산 (p.96-100)

2차원 입력 평면(가로축 Input $x_1$: −1.0~1.0, 세로축 Input $x_2$: −1.0~1.0) 위에서 색상 등고선으로 함수 값을 표시하며 계산을 네 단계로 나누어 보여준다.

- **1단계: 선형 변환 (p.96-97)** — 세 패널 제목은 $\theta_{10}+\theta_{11}x_1+\theta_{12}x_2$ 등. 각 패널은 평행한 등고선이 일정 방향으로 늘어선 기울어진 경사면이며, 세 패널의 등고선 방향이 서로 다름.
- **2단계: 활성화 함수 적용 (p.97-98)** — 각 패널에 굵은 민트색 직선이 하나 나타나고, 그 선을 경계로 한쪽은 평평한 단색 영역(값이 잘린 부분), 다른 쪽은 원래의 경사면 등고선이 남음. 세 패널의 경계선 방향은 각각 다름($h_1$ 우하향, $h_2$ 거의 수직, $h_3$ 완만한 우상향).
- **3단계: Hidden unit에 상수배 적용 (p.98-99)** — 경계선의 위치와 방향은 2단계와 같지만, 등고선 간격과 색의 방향이 바뀌어 각 함수의 기울기 크기·부호가 상수배만큼 달라졌음을 보여줌.
- **4단계: 3개 함수의 합 (p.99-100)** — $y = \phi_0 + \phi_1 h_1 + \phi_2 h_2 + \phi_3 h_3$. 세 개의 민트색 경계선이 한 평면 위에 겹쳐져 입력 평면을 여러 개의 다각형 영역으로 분할하고, 각 영역 안에서는 등고선이 평행한 선형 함수가 됨.

**From Input to Output (p.100)**: 왼쪽은 위 결과의 2차원 등고선 그림, 오른쪽은 같은 함수를 3차원으로 그린 곡면(축: Input 1, Input 2, Output). 3차원 그림에서는 민트색 선을 접는 선(ridge)으로 하여 여러 개의 평평한 면이 이어 붙은 조각선형 곡면임이 보인다.

### 4-4. 임의의 개수의 입력, Hidden Unit, 출력 (p.101)

$D_o$ Outputs, $D$ hidden units, $D_i$ inputs

$$h_d = \mathrm{a}\left[\theta_{d0} + \sum_{i=1}^{D_i} \theta_{di} x_i\right] \qquad y_j = \phi_{j0} + \sum_{d=1}^{D} \phi_{jd} h_d$$

- 예: 3 inputs, 3 hidden units, 2 outputs — $x_1, x_2, x_3$ → $h_1, h_2, h_3$ (완전 연결) → $y_1, y_2$ (완전 연결)

## 5. Deep 네트워크: 합성

### 5-1. 2개의 네트워크를 하나로 합성 (p.103-104)

Network 1:
$$h_1 = \mathrm{a}[\theta_{10} + \theta_{11}x],\quad h_2 = \mathrm{a}[\theta_{20} + \theta_{21}x],\quad h_3 = \mathrm{a}[\theta_{30} + \theta_{31}x]$$
$$y = \phi_0 + \phi_1 h_1 + \phi_2 h_2 + \phi_3 h_3$$

Network 2:
$$h'_1 = \mathrm{a}[\theta'_{10} + \theta'_{11}y],\quad h'_2 = \mathrm{a}[\theta'_{20} + \theta'_{21}y],\quad h'_3 = \mathrm{a}[\theta'_{30} + \theta'_{31}y]$$
$$y' = \phi'_0 + \phi'_1 h'_1 + \phi'_2 h'_2 + \phi'_3 h'_3$$

- 합성 구조 (p.104): $x \to h_1, h_2, h_3 \to y \to h'_1, h'_2, h'_3 \to y'$. 즉 Network 1의 출력 $y$가 Network 2의 입력이 되어 두 네트워크가 하나로 이어진다.

### 5-2. 합성 네트워크의 층(layer)별 출력 (p.105-125)

동일한 슬라이드 구성을 입력값 $x$를 왼쪽에서 오른쪽으로 옮겨가며 반복해 보여주는 애니메이션 시퀀스.

- **a) 네트워크 구조**: $x \to h_1, h_2, h_3 \to y \to h'_1, h'_2, h'_3 \to y'$
- **b) 첫 번째 네트워크의 함수**: 가로축 Input $x$ (−1.0~1.0), 세로축 Output $y$ (−1.0~1.0). $x=-1$에서 $y=-1$로 시작해 $x=0$ 부근에서 $y\approx1$까지 올라갔다가, $x\approx0.5$ 부근에서 $y=-1$까지 내려온 뒤 다시 $x=1$에서 $y\approx1$까지 올라가는 "지그재그" 조각선형 함수
- **c) 두 번째 네트워크의 함수**: 가로축 Input $y$, 세로축 Output $y'$. b)의 출력 $y$가 그대로 c)의 입력이 됨
- **d) 합성 결과**: 가로축 Input $x$, 세로축 Output $y'$. b)와 c)를 거쳐 최종적으로 $x \mapsto y'$ 대응이 만들어짐

각 슬라이드에서 회색 점은 현재 $x$ 값, 민트색 점은 그때의 $y$ 값, 갈색 점은 그때의 $y'$ 값이며, 점선 화살표가 b) → c) → d)로 값이 전달되는 경로를 나타낸다. $x$를 조금씩 증가시키며 같은 과정을 반복하면 d)에 대응점들이 하나씩 누적되어 최종 함수 $x \mapsto y'$의 모양이 그려진다.

**핵심**: 첫 번째 네트워크의 출력 $y$를 두 번째 네트워크의 입력으로 넣어 합성하면, 입력 $x$에서 최종 출력 $y'$로 가는 새로운 조각선형 함수가 만들어진다.

### 5-3. "접기(folding)" 비유 (p.126)

세 장의 종이 그림으로 합성을 설명한다.

- **a)** 가로축 Input $x$, 세로축 Output $y$. 첫 번째 네트워크의 함수(청록색 꺾인 선)를 따라 입력 공간이 아코디언처럼 접힌(folded) 종이로 그려짐
- **b)** 가로축 Input $y$, 세로축 Output $y'$. 접히지 않은 평평한 종이 한 장으로, 두 번째 네트워크가 $y$에 적용하는 함수를 나타냄
- **c)** 가로축 Input $x$, 세로축 Output $y'$. a)의 접힘이 b)에 전달되어, 결과적으로 다시 여러 번 접힌 형태의 종이가 됨

즉 합성은 "입력 공간을 접은 뒤 두 번째 함수를 적용하는 것"으로 비유된다.

### 5-4. Shallow 네트워크와 Deep 네트워크 비교 (p.127)

**Deep(2층) 네트워크** — $x$ → $h_1,h_2,h_3$ → $y$ → $h_1',h_2',h_3'$ → $y'$
- 파라미터 수: 20개
- 조각선형 구역: 최대 16개

**Shallow(1층) 네트워크** — $x$ → $h_1,\dots,h_6$ (hidden unit 6개) → $y$
- 파라미터 수: 19개
- 조각선형 구역: 최대 7개

**결론: Deep 네트워크의 표현력이 높음**

### 5-5. 2D Input: 2개의 네트워크를 하나로 합성 (p.128)

- **a) 구조**: 입력 $x_1, x_2$ → hidden units $h_1, h_2, h_3$ → $y$ → hidden units $h_1', h_2'$ → $y'$
- **b)** Output $y$를 2차원 입력 평면 위에 그린 그림. 굵은 청록색 직선 3개가 평면을 여러 영역으로 분할(각 hidden unit의 꺾임 경계)
- **c)** 가로축 Input $y$, 세로축 Output $y'$. $y \approx -0.4$ 지점에서 꺾여, 그 전에는 완만히 상승하고 그 후에는 하강하는 조각선형 함수
- **d)** Output $y'$를 다시 $x_1$-$x_2$ 평면에 그린 그림. b)의 3개 경계선에 c)의 꺾임에서 생긴 새 경계선들이 추가되어, 평면이 b)보다 더 많은 영역으로 분할됨

## 6. Deep 네트워크 수식 표현

### 6-1~6-2. 합성 수식과 새로운 변수 (p.130-131)

Network 2의 hidden units에 Network 1의 출력 $y$를 대입하면:

$$h_1' = \mathrm{a}[\theta_{10}' + \theta_{11}'y] = \mathrm{a}[\theta_{10}' + \theta_{11}'\phi_0 + \theta_{11}'\phi_1 h_1 + \theta_{11}'\phi_2 h_2 + \theta_{11}'\phi_3 h_3]$$
$$h_2' = \mathrm{a}[\theta_{20}' + \theta_{21}'y] = \mathrm{a}[\theta_{20}' + \theta_{21}'\phi_0 + \theta_{21}'\phi_1 h_1 + \theta_{21}'\phi_2 h_2 + \theta_{21}'\phi_3 h_3]$$
$$h_3' = \mathrm{a}[\theta_{30}' + \theta_{31}'y] = \mathrm{a}[\theta_{30}' + \theta_{31}'\phi_0 + \theta_{31}'\phi_1 h_1 + \theta_{31}'\phi_2 h_2 + \theta_{31}'\phi_3 h_3]$$

전개된 식에서 묶음 항들을 새로운 파라미터 $\psi$로 치환한다 (p.131):

- $\psi_{10} \leftarrow \theta_{10}' + \theta_{11}'\phi_0$
- $\psi_{11} \leftarrow \theta_{11}'\phi_1$
- $\psi_{12} \leftarrow \theta_{11}'\phi_2$
- $\psi_{13} \leftarrow \theta_{11}'\phi_3$

**새로운 파라미터 사용으로 수식의 단순화:**

$$h_1' = \mathrm{a}[\psi_{10} + \psi_{11}h_1 + \psi_{12}h_2 + \psi_{13}h_3]$$
$$h_2' = \mathrm{a}[\psi_{20} + \psi_{21}h_1 + \psi_{22}h_2 + \psi_{23}h_3]$$
$$h_3' = \mathrm{a}[\psi_{30} + \psi_{31}h_1 + \psi_{32}h_2 + \psi_{33}h_3]$$

### 6-3. 2층 네트워크의 표현 (p.132-135)

$$h_1 = \mathrm{a}[\theta_{10}+\theta_{11}x], \quad h_2 = \mathrm{a}[\theta_{20}+\theta_{21}x], \quad h_3 = \mathrm{a}[\theta_{30}+\theta_{31}x]$$
$$y' = \phi_0' + \phi_1'h_1' + \phi_2'h_2' + \phi_3'h_3'$$

**전체를 한 식으로 펼친 형태 (p.133):**

$$
\begin{aligned}
y' = \;& \phi_0' + \phi_1'\mathrm{a}\left[\psi_{10} + \psi_{11}\mathrm{a}[\theta_{10}+\theta_{11}x] + \psi_{12}\mathrm{a}[\theta_{20}+\theta_{21}x] + \psi_{13}\mathrm{a}[\theta_{30}+\theta_{31}x]\right] \\
+\;& \phi_2'\mathrm{a}\left[\psi_{20} + \psi_{21}\mathrm{a}[\theta_{10}+\theta_{11}x] + \psi_{22}\mathrm{a}[\theta_{20}+\theta_{21}x] + \psi_{23}\mathrm{a}[\theta_{30}+\theta_{31}x]\right] \\
+\;& \phi_3'\mathrm{a}\left[\psi_{30} + \psi_{31}\mathrm{a}[\theta_{10}+\theta_{11}x] + \psi_{32}\mathrm{a}[\theta_{20}+\theta_{21}x] + \psi_{33}\mathrm{a}[\theta_{30}+\theta_{31}x]\right]
\end{aligned}
$$

**네트워크를 합성 함수로 봄 (p.134-135)**

> 두 번째 hidden 유닛들의 pre-activation(전활성값)을 살펴보자. 이 시점에서는 출력 3개를 갖는 1-층(one-layer) 네트워크로 볼 수 있다.

### 6-4. 2층 네트워크: 단계별 계산 (p.136-140)

최종 출력 $y'$가 만들어지는 과정을 그래프로 단계별로 추적한다. 모든 그래프의 가로축은 Input $x$ (0.0~2.0), 세로축은 Output (−1.0~1.0).

- **1단계 — 첫 번째 Layer 출력 (pre-activation)**: a) $\psi_{10}+\psi_{11}h_1+\psi_{12}h_2+\psi_{13}h_3$(주황), b) $\psi_{20}+\cdots$(청록), c) $\psi_{30}+\cdots$(짙은 회색). 각각 여러 번 꺾인 조각선형 곡선이며 모양이 서로 다름
- **2단계 — 활성화 함수 적용**: d), e), f)는 각각 a), b), c)의 음수 구간이 모두 0.0으로 잘려(clipped) 평평해지고 양수 구간만 남은 형태
- **3단계 — 두 번째 Hidden unit 상수배 적용**: g) $\phi_1'h_1'$(주황, 음수 상수가 곱해져 위아래가 뒤집힌 모양), h) $\phi_2'h_2'$(청록, 양수 상수배), i) $\phi_3'h_3'$(회색, 크기만 조정)
- **4단계 — 합산**: j) $\phi_0' + \phi_1'h_1' + \phi_2'h_2' + \phi_3'h_3'$. 여러 번 꺾인 조각선형 함수가 되며, $x\approx1.0$ 부근 약 −0.4의 최저점을 지나 이후 톱니 모양으로 진동하며 상승

### 6-5. 용어의 단순화: 벡터·행렬 표현 (p.141-148)

**첫 번째 층의 벡터화 (p.142)**

$$\begin{bmatrix} h_1 \\ h_2 \\ h_3 \end{bmatrix} = \mathbf{a}\left[ \begin{bmatrix} \theta_{10} \\ \theta_{20} \\ \theta_{30} \end{bmatrix} + \begin{bmatrix} \theta_{11} \\ \theta_{21} \\ \theta_{31} \end{bmatrix} x \right]$$

**두 번째 층의 행렬화 (p.143)**

$$\begin{bmatrix} h_1' \\ h_2' \\ h_3' \end{bmatrix} = \mathbf{a}\left[ \begin{bmatrix} \psi_{10} \\ \psi_{20} \\ \psi_{30} \end{bmatrix} + \begin{bmatrix} \psi_{11} & \psi_{12} & \psi_{13} \\ \psi_{21} & \psi_{22} & \psi_{23} \\ \psi_{31} & \psi_{32} & \psi_{33} \end{bmatrix} \begin{bmatrix} h_1 \\ h_2 \\ h_3 \end{bmatrix} \right]$$

**출력층의 벡터화 (p.144)**

$$y' = \phi_0' + \begin{bmatrix} \phi_1' & \phi_2' & \phi_3' \end{bmatrix} \begin{bmatrix} h_1' \\ h_2' \\ h_3' \end{bmatrix}$$

**굵은 기호로의 축약 (p.145)**

$$\mathbf{h} = \mathbf{a}\left[\boldsymbol{\theta}_0 + \boldsymbol{\theta}x\right], \qquad \mathbf{h}' = \mathbf{a}\left[\boldsymbol{\psi}_0 + \boldsymbol{\Psi}\mathbf{h}\right], \qquad y = \phi_0' + \boldsymbol{\phi}'\mathbf{h}'$$

**$\beta, \Omega$ 표기로의 전환 (p.146-147)**

$$\mathbf{h}_1 = \mathbf{a}[\boldsymbol{\beta}_0 + \boldsymbol{\Omega}_0\mathbf{x}], \qquad \mathbf{h}_2 = \mathbf{a}[\boldsymbol{\beta}_1 + \boldsymbol{\Omega}_1\mathbf{h}_1], \qquad \mathbf{y} = \boldsymbol{\beta}_2 + \boldsymbol{\Omega}_2\mathbf{h}_2$$

- $\boldsymbol{\beta}_0$는 **Bias vector**, $\boldsymbol{\Omega}_0$는 **Weight matrix**

**$K$층으로의 일반화 (p.148)**

$$\mathbf{h}_1 = \mathbf{a}[\boldsymbol{\beta}_0 + \boldsymbol{\Omega}_0\mathbf{x}],\quad \mathbf{h}_2 = \mathbf{a}[\boldsymbol{\beta}_1 + \boldsymbol{\Omega}_1\mathbf{h}_1],\quad \cdots,\quad \mathbf{h}_K = \mathbf{a}[\boldsymbol{\beta}_{K-1} + \boldsymbol{\Omega}_{K-1}\mathbf{h}_{K-1}]$$
$$\mathbf{y} = \boldsymbol{\beta}_K + \boldsymbol{\Omega}_K\mathbf{h}_K$$

이를 하나의 중첩된 식으로 쓰면:

$$\mathbf{y} = \boldsymbol{\beta}_K + \boldsymbol{\Omega}_K\mathbf{a}\left[\boldsymbol{\beta}_{K-1} + \boldsymbol{\Omega}_{K-1}\mathbf{a}\left[\dots \boldsymbol{\beta}_2 + \boldsymbol{\Omega}_2\mathbf{a}\left[\boldsymbol{\beta}_1 + \boldsymbol{\Omega}_1\mathbf{a}\left[\boldsymbol{\beta}_0 + \boldsymbol{\Omega}_0\mathbf{x}\right]\right]\dots\right]\right]$$

### 6-6. 네트워크 도식화 (p.149)

3개의 hidden layer를 갖는 네트워크 도식. 각 층 위에는 bias 노드(값 1)가 있고, 주황 화살표는 bias vector $\boldsymbol{\beta}$, 회색 화살표는 weight matrix $\boldsymbol{\Omega}$를 나타낸다.

| 층 | 이름 | 차원 |
|---|---|---|
| 입력 | Input $\mathbf{x}$ ($x_1,x_2,x_3$) | $D_i = 3$ |
| 1 | Hidden layer $\mathbf{h}_1$ | $D_1 = 4$ |
| 2 | Hidden layer $\mathbf{h}_2$ | $D_2 = 2$ |
| 3 | Hidden layer $\mathbf{h}_3$ | $D_3 = 3$ |
| 출력 | Output $\mathbf{y}$ ($y_1,y_2$) | $D_o = 2$ |

파라미터 차원:
- $\boldsymbol{\beta}_0 \in \mathbb{R}^4$, $\boldsymbol{\Omega}_0 \in \mathbb{R}^{4\times3}$
- $\boldsymbol{\beta}_1 \in \mathbb{R}^2$, $\boldsymbol{\Omega}_1 \in \mathbb{R}^{2\times4}$
- $\boldsymbol{\beta}_2 \in \mathbb{R}^3$, $\boldsymbol{\Omega}_2 \in \mathbb{R}^{3\times2}$
- $\boldsymbol{\beta}_3 \in \mathbb{R}^2$, $\boldsymbol{\Omega}_3 \in \mathbb{R}^{2\times3}$

---

# 4차시 — AI & 기계학습 방법론 4: 신경망 적합(Fitting)

## 학습 시작(Overview) — 핵심 질문 (p.152)

- **손실함수란 무엇일까?** 모델이 예측을 얼마나 잘못했는지 측정하는 함수
- **학습이란 무엇일까?** 손실함수를 최소화하는 파라미터(모델의 가중치)를 찾는 과정
- **경사 하강법이란 무엇일까?** 손실함수를 줄이기 위해 기울기를 따라 내려가는 방법
- **확률적 경사 하강법(SGD)의 장점은 무엇일까?** 전체 데이터 대신 일부만 사용하여 업데이트별 계산량이 적은 반면 local 최소점에 빠질 확률이 적은 최적화 방법

## 1. 선형회귀 예시

### 1-1. 손실함수 (p.154)

- 학습 데이터셋: input/output의 $I$개 쌍 — $\{\mathbf{x}_i, \mathbf{y}_i\}_{i=1}^{I}$
- 손실함수(Loss function): 모델이 얼마나 잘못 예측하는지를 측정하는 함수
  - 값이 작을수록 모델이 더 정확하게 학습되었다는 의미
  - 정의: $L\big[\boldsymbol{\phi}, \mathrm{f}\left[\mathbf{x}_i, \boldsymbol{\phi}\right], \{\mathbf{x}_i, \mathbf{y}_i\}_{i=1}^{I}\big]$
  - 간단한 표현: $L[\boldsymbol{\phi}]$ — 입력을 출력에 더 잘 매핑할수록 더 작은 값을 반환하는 스칼라

### 1-2. 학습 (p.155)

학습: 손실함수를 최소화하는 파라미터를 찾음

$$\hat{\phi} = \operatorname*{argmin}_{\phi}\big[L[\phi]\big]$$

- $\hat{\phi}$: 모델의 파라미터(우리가 조정해야 하는 값) 찾기
- $\operatorname{argmin}$: 손실함수 $L[\phi]$를 가장 작게 만드는 파라미터 찾기

### 1-3. 1D 선형회귀 예 (p.156)

- 선형회귀 모델에서 손실함수는 예측값과 실제값의 차이를 제곱하여 합산한 값(최소제곱 손실함수, MSE)
- 학습의 목적은 이 손실함수를 **최소화하는 직선**을 찾는 것

$$L[\phi] = \sum_{i=1}^{I}\big(\mathrm{f}[x_i, \phi] - y_i\big)^2 = \sum_{i=1}^{I}\big(\phi_0 + \phi_1 x_i - y_i\big)^2$$

- $\mathrm{f}[x_i,\phi]$: 모델이 예측한 값, $y_i$: 실제 정답 값

### 1-4. 손실 지형 읽기와 경사 하강법 (p.157-163)

**손실 지형 읽기 (p.157)**

- 등고선(왼쪽 그림): 손실 함수 값의 크기 — 밝을수록 손실이 큼, 어두울수록 손실이 적음
- 주황색 점: 실제 데이터 / 초록색 직선: 현재 파라미터로 만든 모델
- 데이터와 선의 오차가 크다면 손실 값이 큼

> **[그림 a]** 가로축 Intercept $\phi_0$ (0.0~2.0), 세로축 Slope $\phi_1$ (위 −1.0 ~ 아래 1.0). 밝은 좌상단(손실 큼)에서 어두운 우하단 골짜기(손실 작음)로 이어지는 타원형 등고선.
> **[그림 b]** 같은 산점도에 현재 파라미터로 만든 매우 완만한 직선이 데이터 아래를 지나감(오차가 큼).

**경사 하강법(gradient descent) — 반복 진행 (p.158-163)**

경사 하강법: 손실 함수의 값이 줄어드는 방향으로 파라미터를 이동하는 과정. 두 그림이 짝으로 반복되며 스텝이 하나씩 추가된다. 왼쪽 a)는 $(\phi_0, \phi_1)$ 등고선 위 파라미터 궤적, 오른쪽 b)는 그 파라미터가 만드는 직선.

- 점 `0` → `1` → `2` → `3` → `4`로 진행하며 등고선의 어두운 골짜기 안쪽에 도달
- 대응하여 직선도 점점 데이터에 잘 맞아가고, 직선 `4`가 데이터 점들을 가장 잘 관통함

## 2. 수학 리뷰

### 2-1. 미분을 이용한 최적화 (p.164-169)

- 이차 함수 $y = x^2 - 4x + 5$, 그래프는 위로 열린 포물선
- 미분 결과가 기울기를 의미하며, 기울기가 0이 되는 지점이 극값(최소값)

$$\frac{\partial y}{\partial x} = 2x - 4$$

**각 지점에서의 미분 방향과 절대값 (p.165-169)**

- $x = 2$ (최저점): 기울기 0
- $x = 3$: 오른쪽을 향하는 짧은 화살표
- $x = 4$: 오른쪽을 향하는 더 긴 화살표
- $x = 1$: 왼쪽을 향하는 화살표
- $x = 0$: 왼쪽을 향하는 더 긴 화살표

→ 최저점 $x=2$에서 멀어질수록 미분값의 절대값이 커지고, 최저점 좌측에서는 왼쪽, 우측에서는 오른쪽을 향한다.

## 3. 경사하강(Gradient Descent)

### 3-1. 경사 하강 알고리즘 (p.171-172)

경사 하강법은 손실 함수 $L[\phi]$를 최소화하기 위해 파라미터 $\phi$를 반복적으로 갱신하는 알고리즘.

**기울기 계산** — 손실 함수 $L[\phi]$를 파라미터 $\phi$에 대해 편미분

$$\frac{\partial L}{\partial \boldsymbol{\phi}} = \begin{bmatrix} \dfrac{\partial L}{\partial \phi_0} \\[4pt] \dfrac{\partial L}{\partial \phi_1} \\[2pt] \vdots \\[2pt] \dfrac{\partial L}{\partial \phi_N} \end{bmatrix}$$

**파라미터 업데이트** — 기울기(미분값)의 **반대 방향**으로 이동해야 손실 함수가 줄어듦

$$\boldsymbol{\phi} \longleftarrow \boldsymbol{\phi} - \alpha \frac{\partial L}{\partial \boldsymbol{\phi}}$$

- $\alpha \geq 0$ 는 학습률(learning rate)로, 한 번의 스텝에서 이동하는 크기를 결정

**손실 함수의 기울기 계산 (p.172)**

$$L[\phi] = \sum_{i=1}^{I}\ell_i = \sum_{i=1}^{I}\big(\mathrm{f}[x_i, \phi] - y_i\big)^2 = \sum_{i=1}^{I}\big(\phi_0 + \phi_1 x_i - y_i\big)^2$$

- 전체 손실 $L[\phi]$는 모든 데이터 샘플 손실 $\ell_i$의 합, 전체 기울기는 각 데이터의 기울기를 합한 것

$$\frac{\partial L}{\partial \phi} = \frac{\partial}{\partial \phi}\sum_{i=1}^{I}\ell_i = \sum_{i=1}^{I}\frac{\partial \ell_i}{\partial \phi}$$

### 3-2. 경사 하강법: 단계별 계산 (p.173-180) `[HARD]`

**첫 단계: 미분값 구하기**

$$\frac{\partial \ell_i}{\partial \phi} = \begin{bmatrix} \dfrac{\partial \ell_i}{\partial \phi_0} \\[6pt] \dfrac{\partial \ell_i}{\partial \phi_1} \end{bmatrix} = \begin{bmatrix} 2(\phi_0 + \phi_1 x_i - y_i) \\[4pt] 2x_i(\phi_0 + \phi_1 x_i - y_i) \end{bmatrix}$$

- 등고선 그림에서 점 주위로 여러 방향의 화살표가 뻗어 있고, 그중 하나에 "손실이 가장 빠르게 증가하는 방향"이라는 주석

**두 번째 단계: 파라미터 업데이트**

$$\phi \longleftarrow \phi - \alpha\frac{\partial L}{\partial \phi}$$

- $\alpha$ = 학습률 (Learning rate)
- 미분값의 반대 방향으로 이동(손실 최소화)
- 업데이트를 반복하면 궤적이 `0` → `1` → `2` → … 로 이어지며 등고선의 더 어두운(손실이 작은) 안쪽 영역으로 진입

### 3-3. 한눈으로 보는 경사 하강법 (p.184)

- a) 데이터 — Input $x$ vs Output $y$ 산점도
- b) 손실 함수 3D 그래프 — 축은 Slope $\phi_1$ / Intercept $\phi_0$, 높이는 Loss $L[\phi]$(눈금 10~70). 곡면 위에 점 0~4의 경로 표시
- c) 손실 함수 등고선(2D) — 같은 경로를 등고선 위에 투영, 화살표로 0→1→2→3→4 진행 방향 표시
- d) 선형함수의 업데이트 — 각 스텝의 직선이 데이터에 맞춰져 가는 모습

### 3-4. 함수에 따른 최적화 난이도: Convex vs Non-convex (p.185-186)

- **Convex**: 곡선이 항상 U자처럼 아래로 볼록. 그래프 위 임의 두 점을 잇는 직선이 그래프 위(또는 같은 위치)에 있음
- **Non-convex**: 봉우리·골짜기·오목한 구간이 섞인 모양. 두 점을 이은 직선이 그래프 아래로 내려가는 구간이 생김

손실 함수 모양에 따라 최적화의 난이도가 달라짐:

- **Convex 문제**: **전역(global) 최소값이 유일**함 → 최적화가 쉬움
- **Non-Convex 문제**: **여러 개의 지역(local) 최소값** 또는 **새들(saddle)점**이 있음 → 최적화가 어려움

## 4. 확률적 경사 하강법 (Stochastic Gradient Descent)

### 4-1. 경사 하강법의 단점 (p.188-189)

- Non-convex 문제에서 **지역(local) 최소점에 빠지기 쉬움**
  - 점 2에서 출발한 경사하강 방법은 local 최소점에 빠짐
- 매 스텝마다 **전체 데이터에 대한 미분값을 구하여 업데이트**함. 스텝별 계산량이 많음

**대안 (p.189)**: 전체 데이터를 한 번에 쓰는 대신, **무작위로 선택한 데이터 샘플** 사용 → **확률적 경사 하강법(Stochastic Gradient Descent, SGD)**

> **[그림4-1]** 가로축 $\phi_0$ (−10~10), 세로축 $\phi_1$ (2.5~22.5)의 Loss 등고선 지도. 세로로 길쭉한 골짜기들이 여러 개 반복되는 non-convex 지형이며, 점 1에서 출발한 경로는 지그재그로 내려가고, 점 2에서 출발한 경로는 점 3 위치의 골짜기(local 최소점)에 멈춤.

### 4-2. 확률적 경사 하강법의 개념 (p.190)

**경사 하강법(Gradient Descent)**: **전체 데이터셋을 사용**하여 기울기를 계산하고 파라미터 업데이트

$$\phi_{t+1} \longleftarrow \phi_t - \alpha \sum_{i=1}^{I} \frac{\partial \ell_i[\phi_t]}{\partial \phi}$$

**확률적 경사 하강법(SGD, Mini-batch 버전)**: **무작위 확률로 샘플**된 일부 데이터(batch)만 사용하여 기울기 계산

$$\phi_{t+1} \longleftarrow \phi_t - \alpha \sum_{i \in \mathcal{B}_t} \frac{\partial \ell_i[\phi_t]}{\partial \phi}$$

두 식의 차이는 합의 범위($i=1 \sim I$ 전체 vs $i \in \mathcal{B}_t$ 배치)에 있다.

### 4-3~4-4. SGD의 특성 (p.191-193)

> **[그림4-2]** 동일한 Loss 등고선 지도 위에 두 경로 비교. a) "Gradient descent": 점 1과 점 2에서 출발한 경로가 비교적 매끄럽게 진행되어 점 3에서 멈춤. b) "Stochastic gradient descent": 점 1에서 출발한 경로가 심하게 떨리며(지그재그) 골짜기 사이를 오가고, 점 2에서 출발한 경로도 넓게 요동치며 이동.

**정리된 특성 (p.193)**

- **국소 최솟값**
  - 전체 데이터가 아닌 일부 배치로 기울기를 계산하기 때문에 노이즈가 섞여 있음
  - 노이즈가 오히려 local minima, saddle point에서 빠져나오는 데 도움이 됨
- **노이즈가 있지만 여전히 타당한 업데이트**
  - 미니배치의 기울기는 정확한 전체 기울기가 아니지만, 평균적으로 올바른 방향을 가리킴
  - 학습이 점진적으로 최적점 방향으로 수렴
- **계산 비용 절감**
  - 전체 데이터셋에서 작은 배치만 사용하므로 반복(스텝)당 연산량이 적음
  - 큰 데이터셋에서 효율적으로 학습 가능
- **수렴 특성**
  - full batch처럼 매끄럽게 수렴하지 않고, 무작위성 때문에 더 많이 진동(jitter, 지그재그)하면서 움직임
  - 전역 최솟값 근처의 좋은 해에 도달할 수 있음. Convex 문제에서는 full-batch 경사하강보다 수렴이 늦을 수 있음

## 5. 역전파 (Backpropagation)

### 5-1. 네트워크 파라미터의 미분 (p.195-198)

- Layer별로 파라미터들이 존재함. 서로 영향을 미침
- **네트워크는 합성함수임**

네트워크의 순전파 정의:

$$\mathbf{f}_0 = \boldsymbol{\beta}_0 + \boldsymbol{\Omega}_0 \mathbf{x}_i,\qquad \mathbf{h}_1 = \mathbf{a}[\mathbf{f}_0]$$
$$\mathbf{f}_1 = \boldsymbol{\beta}_1 + \boldsymbol{\Omega}_1 \mathbf{h}_1,\qquad \mathbf{h}_2 = \mathbf{a}[\mathbf{f}_1]$$
$$\mathbf{f}_2 = \boldsymbol{\beta}_2 + \boldsymbol{\Omega}_2 \mathbf{h}_2,\qquad \mathbf{h}_3 = \mathbf{a}[\mathbf{f}_2]$$
$$\mathbf{f}_3 = \boldsymbol{\beta}_3 + \boldsymbol{\Omega}_3 \mathbf{h}_3,\qquad \ell_i = \mathrm{l}[\mathbf{f}_3, y_i]$$

> **[그림]** Training input $\mathbf{x}$ (노드 3개) → Hidden layer $\mathbf{h}_1$ (4개) → $\mathbf{h}_2$ (2개) → $\mathbf{h}_3$ (3개) → Output $\mathbf{f}[\mathbf{x}, \boldsymbol{\phi}]$ (2개) → Loss $l$. 층 사이의 가중치는 $\boldsymbol{\Omega}_0, \boldsymbol{\Omega}_1, \boldsymbol{\Omega}_2, \boldsymbol{\Omega}_3$. 슬라이드가 넘어가며 특정 연결 하나에서 시작해 그 뒤의 모든 경로가 주황색으로 확장되어, 한 파라미터의 변화가 뒤쪽 층 전체를 거쳐 손실까지 영향을 미침을 보여줌.

### 5-2. 합성함수의 미분: 연쇄법칙 (p.199)

파라미터 $\Omega$를 갖는 합성 함수 $y = f_1\big(f_0(x;\ \boldsymbol{\Omega})\big)$ 를 미분하면?

핵심 아이디어: **합성함수의 변화율 = 바깥의 변화율 × 안쪽의 변화율**

$$\frac{\partial y}{\partial \boldsymbol{\Omega}} = \frac{\partial f_1\big(f_0(x;\boldsymbol{\Omega})\big)}{\partial f_0(x;\boldsymbol{\Omega})} \cdot \frac{\partial f_0(x;\boldsymbol{\Omega})}{\partial \boldsymbol{\Omega}}$$

해석:
- $f_1$의 **입력(= $f_0$)에 대한 기울기**를 구한 뒤,
- 그 값을 $f_0$의 **파라미터 $\Omega$에 대한 기울기**와 곱한다.
- $x$는 파라미터가 아니므로 $\partial y/\partial x$는 여기서 필요 없음.

### 5-3. 역전파(Backpropagation) (p.200-208)

**역전파란?** 출력 오차를 기준으로 **그래프를 거꾸로** 따라가며 **연쇄법칙**으로 각 노드(파라미터 포함)의 미분값을 계산하는 절차

**절차**
1. 각 단계별 계산을 분해
2. 각 layer별 값($\mathbf{f}_k$)을 계산
3. 각 layer별 값($\mathbf{f}_k$)에 대한 출력 손실($\ell_i$)의 미분을 구함
4. 파라미터에 대한 미분을 구함

**3단계: layer 값에 대한 손실의 미분 (p.200-206)**

$$\frac{\partial \ell_i}{\partial \mathbf{f}_0} = \frac{\partial \mathbf{h}_1}{\partial \mathbf{f}_0} \frac{\partial \mathbf{f}_1}{\partial \mathbf{h}_1} \frac{\partial \ell_i}{\partial \mathbf{f}_1}$$
$$\frac{\partial \ell_i}{\partial \mathbf{f}_1} = \frac{\partial \mathbf{h}_2}{\partial \mathbf{f}_1} \frac{\partial \mathbf{f}_2}{\partial \mathbf{h}_2} \frac{\partial \ell_i}{\partial \mathbf{f}_2}$$
$$\frac{\partial \ell_i}{\partial \mathbf{f}_2} = \frac{\partial \mathbf{h}_3}{\partial \mathbf{f}_2} \frac{\partial \mathbf{f}_3}{\partial \mathbf{h}_3} \frac{\partial \ell_i}{\partial \mathbf{f}_3}$$

**재사용 구조**: 뒤쪽에서 이미 구한 항을 앞쪽 식에 그대로 대입할 수 있음.

$$\frac{\partial \ell_i}{\partial \mathbf{f}_1} = \frac{\partial \mathbf{h}_2}{\partial \mathbf{f}_1} \frac{\partial \mathbf{f}_2}{\partial \mathbf{h}_2} \left( \frac{\partial \mathbf{h}_3}{\partial \mathbf{f}_2} \frac{\partial \mathbf{f}_3}{\partial \mathbf{h}_3} \frac{\partial \ell_i}{\partial \mathbf{f}_3} \right)$$

$$\frac{\partial \ell_i}{\partial \mathbf{f}_0} = \frac{\partial \mathbf{h}_1}{\partial \mathbf{f}_0} \frac{\partial \mathbf{f}_1}{\partial \mathbf{h}_1} \left( \frac{\partial \mathbf{h}_2}{\partial \mathbf{f}_1} \frac{\partial \mathbf{f}_2}{\partial \mathbf{h}_2} \frac{\partial \mathbf{h}_3}{\partial \mathbf{f}_2} \frac{\partial \mathbf{f}_3}{\partial \mathbf{h}_3} \frac{\partial \ell_i}{\partial \mathbf{f}_3} \right)$$

개별 항의 계산 예 (p.206):

$$\frac{\partial \mathbf{f}_3}{\partial \mathbf{h}_3} = \frac{\partial}{\partial \mathbf{h}_3}\left(\boldsymbol{\beta}_3 + \boldsymbol{\Omega}_3 \mathbf{h}_3\right) = \boldsymbol{\Omega}_3^{T}$$

**4단계: 파라미터에 대한 미분 (p.207)**

$$\frac{\partial \ell_i}{\partial \boldsymbol{\Omega}_k} = \frac{\partial \mathbf{f}_k}{\partial \boldsymbol{\Omega}_k} \frac{\partial \ell_i}{\partial \mathbf{f}_k} = \frac{\partial}{\partial \boldsymbol{\Omega}_k}\left(\boldsymbol{\beta}_k + \boldsymbol{\Omega}_k \mathbf{h}_k\right) \frac{\partial \ell_i}{\partial \mathbf{f}_k} = \frac{\partial \ell_i}{\partial \mathbf{f}_k} \mathbf{h}_k^{T}$$

$$\frac{\partial \ell_i}{\partial \boldsymbol{\beta}_k} = \frac{\partial \mathbf{f}_k}{\partial \boldsymbol{\beta}_k} \frac{\partial \ell_i}{\partial \mathbf{f}_k} = \frac{\partial}{\partial \boldsymbol{\beta}_k}\left(\boldsymbol{\beta}_k + \boldsymbol{\Omega}_k \mathbf{h}_k\right) \frac{\partial \ell_i}{\partial \mathbf{f}_k} = \frac{\partial \ell_i}{\partial \mathbf{f}_k}$$

**역전파 계산 그래프 (p.208)**

손실에서 입력 방향으로 화살표가 거꾸로 흐르는 그래프:

$\ell_i \rightarrow \dfrac{\partial \ell_i}{\partial \mathbf{f}_3} \rightarrow \dfrac{\partial \ell_i}{\partial \mathbf{h}_3} \rightarrow \dfrac{\partial \ell_i}{\partial \mathbf{f}_2} \rightarrow \dfrac{\partial \ell_i}{\partial \mathbf{h}_2} \rightarrow \dfrac{\partial \ell_i}{\partial \mathbf{f}_1} \rightarrow \dfrac{\partial \ell_i}{\partial \mathbf{h}_1} \rightarrow \dfrac{\partial \ell_i}{\partial \mathbf{f}_0}$, 그리고 맨 왼쪽에 $\mathbf{x}_i$.

각 $\dfrac{\partial \ell_i}{\partial \mathbf{f}_k}$ 노드에서 아래로 두 갈래가 뻗어 나와 $\dfrac{\partial \ell_i}{\partial \boldsymbol{\beta}_k}$ 와 $\dfrac{\partial \ell_i}{\partial \boldsymbol{\Omega}_k}$ ($k = 0, 1, 2, 3$)를 만든다.

> 손실로부터 각 layer별 파라미터의 미분을 거꾸로 구할 수 있음

## 강의 정리 (4차시) (p.209)

**경사 하강법 (Gradient Descent)**
- Step 1: 손실함수의 기울기(미분값) 계산
- Step 2: 기울기의 반대 방향으로 파라미터 업데이트
- 학습률($\alpha$): 한 번에 이동하는 크기 결정

**Convex 문제와 최적화**
- 2차 미분이 항상 양수 → 아래로 볼록한 형태
- 국소 최솟값 = 전역 최솟값 → 안정적인 학습 가능

**확률적 경사 하강법 (SGD)**
- 전체가 아닌 일부(미니배치) 데이터로 기울기 계산 → 계산 비용 감소
- 매 업데이트마다 노이즈가 섞여 국소 최솟값에서 벗어나기 유리

## 확인문제 (4차시) (p.210-213)

**1. 손실함수(Loss Function)의 역할로 가장 알맞은 것은?**
a. 입력 데이터를 무작위로 섞는다. / b. 모델의 예측이 얼마나 잘못되었는지 측정한다. / c. 파라미터를 업데이트하는 규칙을 제공한다. / d. 학습률(learning rate)을 설정한다.

**2. 경사 하강법에서 'Step 1'에 해당하는 단계는?**
a. 파라미터를 무작위로 초기화 / b. 손실함수를 최소화 / c. 파라미터에 대한 기울기(미분값)를 계산 / d. 데이터를 섞어 미니배치를 생성

**3. 확률적 경사 하강법(SGD)의 장점은?**
a. 항상 전역 최적해에 도달한다. / b. 계산 비용이 커서 정밀하다. / c. 일부 데이터만으로 업데이트해 연산 비용이 작다. / d. 항상 같은 결과로 수렴한다.

**4. 다음 중 Convex 문제의 특징으로 맞는 것은?**
a. 2차 미분이 항상 0이다. / b. 2차 미분이 모든 구간에서 음수이다. / c. 2차 미분이 모든 구간에서 양수이다. / d. 미분값이 존재하지 않는다.

**5.** 학습 과정에서 손실함수를 최소화하기 위해 조정하는 값은 무엇인가요?
**6.** 경사 하강법에서 파라미터를 업데이트 시 사용하는 고정된 비율은?
**7.** SGD가 경사 하강법(full-batch GD)보다 항상 수렴 속도가 빠른가?
**8.** SGD는 국소 최솟값(local minima)에서 벗어날 수 있는 장점이 있다. 그 이유는?

**정답 (1~4)**: 1. b / 2. c / 3. c / 4. c

**해설**
1. 손실함수는 모델 예측값과 실제 정답 사이의 차이를 수치화한 지표다. 이 값이 작아질수록 모델의 예측 성능이 좋아졌음을 의미하며, 학습은 손실을 최소화하는 방향으로 진행된다.
2. 경사 하강법의 첫 단계는 현재 파라미터에서 손실함수의 기울기를 계산하는 것이다. 이 기울기는 손실이 가장 빨리 줄어드는 방향을 알려주며, 이후 파라미터를 이 방향으로 이동시킨다.
3. SGD는 전체 데이터가 아니라 일부 샘플(미니배치)을 사용해 기울기를 계산한다. 덕분에 계산량이 적어 속도가 빠르고, 대규모 데이터셋에도 효율적으로 적용할 수 있다.
4. Convex 문제는 손실함수가 아래로 볼록한 형태로, 2차 미분이 항상 양수이거나 0이다. 따라서 어떤 국소 최소점도 전역 최소점과 동일하다.

**정답 (5~8)**: 5. 파라미터(Parameters) / 6. 학습률(Learning Rate, α) / 7. 에폭(Epoch) / 8. 노이즈 섞인 업데이트로 탐색 가능

**해설**
5. 학습 과정에서 실제로 조정되는 것은 모델의 파라미터(가중치, 편향)이다. 이 값을 반복적으로 갱신하여 손실이 최소화되도록 한다.
6. 학습률(learning rate)은 파라미터를 한 번 업데이트할 때 이동하는 거리의 비율이다. 값이 너무 크면 발산할 수 있고, 너무 작으면 학습이 느려진다.
7. 항상 그렇지 않다. 예를 들어, convex 문제(또는 convex에 가까운 경우)에서는 오히려 GD의 수렴 속도가 빠를 수 있다. SGD가 수렴 속도가 빠르다고 말하면 안 된다.
8. SGD는 미니배치를 무작위로 선택하기 때문에 기울기에 노이즈가 포함된다. 이 특성 덕분에 국소 최솟값이나 안장점(saddle point)에 머무르지 않고 더 넓은 영역을 탐색할 수 있다.

---

# 부록

## 참고 자료 목록 (p.216-220)

| No. | 차시 | 목차 | 내용 |
|---|---|---|---|
| 1 | 1차시 | 1. 선형 회귀 | 최소제곱회귀의 개념을 단순 선형 회귀부터 다중 선형 회귀까지 손실 함수(MSE), 정규 방정식, 선형대수 관점에서 수식과 함께 설명 |
| 2 | 1차시 | 2. 단순선형회귀 | MSE 손실 함수와 경사하강법(BGD)을 이용하여 w·b를 업데이트하는 과정을 수식과 파이썬 코드로 직접 구현하며 설명 |
| 3 | 1차시 | 2. 단순선형회귀 | 최소제곱 회귀로 데이터에 가장 적합한 선을 찾는 방법, 선형 모델의 필수 조건, 계수 해석, $R^2$를 통한 적합의 강도 측정을 설명하는 영상 |
| 4 | 1차시 | 3. 다중선형회귀 | 다중 선형 회귀의 개념과 가정, 사이킷런의 PolynomialFeatures를 활용한 특성 공학 실습, 특성이 과도하게 늘어날 때 발생하는 과적합 문제를 코드로 설명 |
| 5 | 1차시 | 3. 다중선형회귀 | 여러 독립 변수로 종속 변수를 예측하는 다중 선형 회귀의 개념과 적용 가정, Python 구현 방법을 설명 |
| 6 | 2차시 | 1. 분류(Classification) | 피처를 기반으로 타겟을 예측하는 분류의 개념과 지도학습 과정, 평가 지표의 필요성을 설명 |
| 7 | 2차시 | 2. 로지스틱회귀 | 로지스틱 회귀의 개념과 수학적 원리, scikit-learn·StatsModels를 활용한 Python 실습 및 분류 문제를 단계적으로 구현하는 방법 |
| 8 | 2차시 | 2. 로지스틱회귀 | 선형 분류 모델과 로지스틱 회귀, 로짓, 시그모이드, 결정 경계 등 다양한 개념을 설명하는 비전공자를 위한 영상 |
| 9 | 2차시 | 2. 로지스틱회귀 | 로지스틱 회귀의 수학적 원리(로지스틱 함수, 오즈, 최대우도법)를 선형 회귀와 비교하며 이론적으로 학습 |
| 10 | 3차시 | 1. 모수적 함수로서의 선형 모델 | 머신러닝과 딥러닝의 차이, 데이터 수에 따른 모델 선택 기준과 모수적·비모수적 방법의 개념 및 장단점 |
| 11 | 3차시 | 2. Shallow 네트워크 | 얕은 신경망의 기본 개념, 선형 회귀의 한계를 넘어 임의의 복잡한 입력-출력 매핑을 처리하는 유연성, 활성화 함수와 유니버설 근사 정리의 중요성을 설명하는 영상 |
| 12 | 4차시 | 1. 선형회귀 예시 | 경사 하강법의 수학적 원리, 머신러닝 모델 최적화에 어떻게 활용되는지 학습 |
| 13 | 4차시 | 1. 선형회귀 예시 | 경사 하강법의 개념과 동작 원리, 학습률(Step size) 설정 문제 및 Local minima 등 주요 문제점 |
| 14 | 4차시 | 3. 경사하강(Gradient Descent) | Understanding Deep Learning 교재의 실습 노트북으로, 선형 모델에 경사 하강법을 직접 구현하며 파라미터 최적화 과정을 코드로 체험 |

(각 항목에는 자료 접근용 QR 코드(URL)가 함께 제시됨)

## 실습 미리보기: MLP 구현 (p.222-225)

**학습 주제: 다층 퍼셉트론(Multilayer perceptron, MLP)을 직접 학습시켜 보자**

- 순전파(forwardpropagation)와 역전파(backpropagation)를 배울 수 있어요 — 순전파, 역전파, `__call__` 매직 메서드
- PyTorch를 이용해서 MLP 아키텍처를 설계할 수 있어요 — 다층 퍼셉트론(MLP), PyTorch 라이브러리, `nn.Module` 클래스
- 학습·검증 루프를 구성하고 모델 평가 방법을 익힐 수 있어요 — 학습·검증 루프, Early Stopping

### 순전파와 역전파 (p.223)

- **순전파(forwardpropagation)**: 입력값으로부터 모델의 각 층마다 존재하는 가중치와 연산하고 활성함수를 통과시키는 과정을 거친 후, 실제 label과 오차를 계산하는 것
- **역전파(backpropagation)**: 순전파의 역순으로, 순전파를 통해 나온 오차를 활용하여 모델의 각 층의 가중치와 편향을 최적화하는 것

### 매직 메서드와 `__call__` 매직 메서드 (p.224)

- **매직 메서드**: 파이썬의 객체에 기본적으로 존재하는 함수
  - 특징: `__`로 메서드의 시작과 끝이 감싸져 있으며, 파이썬의 인터프리터에 의해 자동으로 호출됨
  - 대표적인 매직 메서드: `__init__`
- **`__call__` 매직 메서드**: 어떤 객체에 `()`가 붙었을 때 호출되는 메서드

### 다층 퍼셉트론(MLP)·PyTorch·torch.nn.Module (p.225)

- **다층 퍼셉트론(MLP)**: 입력층, 하나의 은닉층, 출력층으로 구성된 순방향 인공 신경망
- **PyTorch 라이브러리**: Python을 위한 오픈소스 머신러닝 라이브러리
- **torch.nn.Module**: PyTorch 라이브러리 내 모든 종류의 신경망의 기본 클래스
  - `forward` 메서드를 오버라이드하면 `__call__` 매직 메서드에서 호출됨
