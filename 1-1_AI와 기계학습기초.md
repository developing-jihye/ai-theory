# 1-1_AI와 기계학습기초

> **이 문서에 대하여 (튜터용 메타 정보)**
> SSAFY 강의 교재 「1-1_AI와 기계학습기초」(총 179장) 전체를 개념 단위로 정리한 노트입니다.
> 원본은 4개 차시가 하나로 묶여 있습니다. `(p.NN)`은 원본 PDF/이미지의 **장 번호**입니다.
> 이 문서 밖의 지식은 수업 범위가 아닙니다. 설명은 여기 적힌 내용 안에서만 해 주세요.

**전체 구성**

| 차시 | 주제 | 범위 |
|---|---|---|
| 1차시 | AI와 ML은 무엇인가? | p.1-50 |
| 2차시 | 지도학습은 무엇인가? | p.51-103 |
| 3차시 | 교차검증(Cross-Validation) | p.104-134 |
| 4차시 | 비지도 학습은 무엇인가? | p.135-167 |
| 부록 | 참고 자료 / 실습 미리보기 | p.170-179 |

---

# 1차시 — AI & 기계학습 기초 1: AI와 ML은 무엇인가?

## 학습 목표 (p.4)

- AI, ML, DL의 정의 및 관계를 학습합니다.
- 데이터(feature 및 label), 가설공간, 모델(model)의 개념을 학습합니다.
- 학습(learning)의 정의를 학습합니다.

## AI와 머신러닝(ML)이 내 일상에 미치는 영향 (p.6)

- 유튜브는 어떻게 내 취향을 알아맞힐까?
- 이메일은 어떻게 스팸/정상을 가려낼까?
- **공통점**: 사람이 모든 규칙을 미리 코딩하지 않아도, 데이터에서 **규칙**을 학습해 성능을 향상함.

> **[그림1-1] 유튜브 알고리즘** — 유튜브 재생 화면 옆에 추천 영상 목록이 붙어 있는 그림.
> **[그림1-2] 이메일 스팸 구분** — 여러 개의 메일(SPAM 표시된 것과 일반 메일)이 `CLASSIFIER` 상자로 들어가고, 결과가 `INBOX`(노란 메일 묶음)와 `SPAM FOLDER`(빨간 메일 묶음)로 나뉘어 나가는 흐름도.

## 머신러닝의 순환(loop): 반복적 개선 루프 (p.7, p.13-14, p.27, p.31, p.108)

- 흐름도: **데이터 → 모델 → 학습 → 평가**로 화살표가 이어지고, **평가**에서 다시 **모델**로 되돌아가는 피드백 화살표가 있음.
- 머신러닝: 데이터를 이용해 모델을 학습하고, 평가 결과를 바탕으로 데이터·모델·학습 방법을 반복적으로 개선하는 과정
- 이 흐름도는 단원마다 반복 등장하며, 현재 다루는 단계가 초록색으로 강조됨: p.14 **데이터**, p.27 **모델**, p.31 **학습**, p.108 **평가**.

## 1. AI, ML, DL의 정의 (p.9-11)

### AI(Artificial Intelligence)

- 주어진 환경/데이터를 인지·학습·추론을 통해 목표 달성을 하도록 예측·행동 선택·계획하는 시스템

### ML(Machine Learning)

- AI 범주 내에서 데이터로부터 학습하여 목적을 달성하는 접근 방법론
- 예: 생성형 AI, 언어 모델, 이미지 분류 모델, 추천 시스템

### DL(Deep Learning)

- ML 범주 내에서 신경망(Neural Network) 함수를 사용한 학습 방법론

### AI − ML (ML이 아닌 AI 시스템)의 예

- 규칙 기반 시스템
- 휴리스틱 기반 (최적화)알고리즘

> **[그림]** 세 개의 동심원 다이어그램: 가장 바깥 큰 원이 `AI`, 그 안쪽 원이 `ML`, 가장 안쪽 작은 원이 `DL`. 즉 DL ⊂ ML ⊂ AI의 포함 관계를 나타냄. 화살표가 ML 설명은 안쪽 ML/DL 영역을, DL 설명은 DL 원을, "AI − ML의 예"는 AI 원의 바깥 고리 부분을 가리킴.

## 2-1. 데이터 구성요소 (Feature/Label) (p.16)

### 데이터가 왜 중요한가?

- 머신러닝은 규칙을 직접 코딩하지 않고, 데이터에서 규칙을 학습
- 데이터(Feature, Label)의 분포와 관계가 머신러닝의 학습 결과를 결정

### Feature (피처, 특성)

- 모델이 예측에 사용하는 입력정보
- 예측, 판단의 근거/단서

### Label (라벨, 목표값)

- 모델이 예측하려는 정답
- 학습의 목표값

> **[그림2-1] 데이터 구성요소** — ① 데이터셋 격자에서 파란색 열들은 `Features`, 옆의 노란색 열은 `Label`로 표시됨. 설명 `Use a trained ML Model to predict a value from a given dataset with labeled examples.` Feature 부분은 학습된 모델(빨간 상자)로 들어가고, ② `Compare Predicted Values with Actual Values.` — 모델이 낸 `Predicted Values`(청록 삼각형들)와 데이터의 `Actual Values`(노란 원들)를 나란히 비교하는 구조.
> 출처: https://developers.google.com/machine-learning/intro-to-ml/supervised?hl=ko

## 2-2. ML 실생활 예시 (p.18)

### 예시 1 – 유튜브 추천

- Feature: 각 영상들의 정보(장르, 크리에이터, 조회수, 좋아요 수 등), 사용자 정보(시청 이력, 구독 채널 등)
- Label: 영상에 대한 사용자 피드백(시청 여부, 좋아요 클릭 여부)

### 예시 2 – 스팸메일 분류

- Feature: 메일 제목, 발신자, 단어 빈도
- Label: 스팸 / 정상

> **[그림2-2] 유튜브 추천** — 왼쪽 `Feature` 상자에 시청 이력(눈 아이콘), 시청 시간(시계 아이콘), 좋아요 여부(하트 아이콘)가 들어 있고, 화살표가 오른쪽 `Label` 상자 두 개 — 좋아하는지(웃는 얼굴), 싫어하는지(찡그린 얼굴) — 로 향함.

## 3-1. 1D 피쳐 기반 학습 (p.21-25)

### 1D 피쳐 기반 학습(단일 피쳐 학습)이란?

- 1D = 1차원
- Feature가 하나일 때 머신러닝이 학습하는 가장 단순한 형태

### 데이터 생성 모형 (p.22-23)

$$\text{Income}_i = f^{*}(\text{Years of Education}_i) + \epsilon_i$$

- $f^{*}$ : 미지의 참 함수
- $\epsilon_i$ : 측정 오차
- 데이터셋 $D$: 30명의 Years of Education (피쳐)와 Income (라벨) 쌍
- $D = \{(\text{Years of Education}_i, \text{Income}_i)\}_{i=1}^{30}$

**미지의 함수 $(f^{*})$**

- Feature와 Label 사이의 실제 평균 관계
- 하지만 직접 관측할 수 없음
- 오차가 포함된 데이터(점)만 관측 가능

**측정오차 $(\epsilon)$**

- 데이터에는 주로 측정 오차가 섞여 있음
- 원인: 측정 기기의 한계, 환경적 요인 등
- 따라서, 데이터 = 참 함수 + 오차 $(f^{*} + \epsilon)$

> **[그림3-1] 교육 연수와 수입 데이터** — x축 `Years of Education`(10~22), y축 `Income`(약 15~80)의 산점도로 빨간 점 30개가 우상향으로 분포. p.22-23에서는 그 위에 완만한 S자 모양의 파란 곡선이 겹쳐 그려지고 `f*`로 라벨링되며, 한 데이터 점과 곡선 사이의 수직 간격이 $\epsilon_i$로 표시되어 "관측값 = 곡선 + 오차"임을 보여줌.

### 피쳐와 라벨의 관계를 잘 나타낸 함수 $f$는 무엇일까? (p.24-25)

- 데이터를 설명하는 여러 함수 후보가 존재
- 어떤 함수가 가장 잘 맞는지 **학습**해야 함

> **[그림] (p.25)** — 동일한 산점도 위에 서로 다른 보라색 곡선을 얹은 세 패널: **후보모델 1** 데이터보다 완만한 기울기의 직선, **후보모델 2** 데이터 추세에 가깝게 맞춰진 가파른 직선, **후보모델 3** S자 형태의 비선형 곡선으로 점들의 흐름을 가장 촘촘히 따라감. 세 후보 모두 같은 데이터를 설명하려는 서로 다른 함수임을 보여줌.

## 3-2. 모델과 가설 공간 (p.28-29)

### 학습 (Learning)

- "입력(Feature) → 출력(Label)" 관계를 찾는 과정
- 평균 관계를 하나의 함수로 표현함
- 하지만 관계를 표현할 수 있는 함수는 무수히 많음

### 가설 공간 (Hypothesis Space)

- 관계를 표현할 수 있는 모든 후보 함수들의 모음
- **피쳐 공간과 라벨 공간** 위에서 정의된 함수들의 집합 $\mathcal{F}$

### 모델 (Model)

- 가설공간 $\mathcal{F}$에 속한 특정 함수 $f$

> **[그림] (p.28)** — 산점도 위에 여러 개의 하늘색 직선이 부채처럼 겹쳐 그려져 있고, 그 중 하나가 진한 보라색으로 강조됨. 범례 — **가설 공간의 예**: 무수히 많은 선형 함수들의 집합 $\mathcal{F}$ / **모델의 예**: 특정 선형 함수 $f$.
>
> **[그림] (p.29)** — 세 개의 산점도 비교. 왼쪽 두 개는 `𝓕: 선형함수 가설공간`으로 각각 서로 다른 기울기의 보라색 직선(=모델)이 그려져 있고(첫 패널에는 빨간 점들을 가리키는 `데이터` 주석과 직선을 가리키는 `모델` 주석), 오른쪽 패널은 `𝓕: 비선형함수 가설공간`으로 S자 곡선 `모델`이 그려져 있음. 같은 데이터라도 가설공간을 어떻게 잡느냐에 따라 뽑히는 모델이 달라짐을 보여줌.

## 3-3. 학습이란 (p.31-33)

### 학습의 정의와 3요소 (p.32)

- 학습: 주어진 데이터에서 정답을 가장 잘 맞출 수 있도록 모델의 규칙을 조금씩 조정해가는 과정
- 데이터 $D$ → 가설공간 $\mathcal{F}$ → 선택된 모델 $f$

**학습에 필요한 3가지**

| 요소 | 설명 |
|---|---|
| 1. 데이터 (Data) | 학습할 예시들 (학력-수입 쌍 30개) / 입력(특성)과 정답(라벨) 쌍으로 된 정답 모음 |
| 2. 가설 공간(Hypothesis Space) | 선택할 수 있는 모든 후보 함수들의 집합 / "이 중에서 가장 좋은 함수를 찾아야 해" |
| 3. 선택 기준(손실 함수) | 어떤 함수가 더 좋은지 판단하는 척도 / 예측값과 실제값의 차이를 측정 |

**학습 과정**

1. 가설공간에서 하나의 함수를 선택
2. 그 함수로 데이터의 모든 예시를 예측
3. 손실함수로 틀린 정도 계산
4. 더 적게 틀리도록 함수의 파라미터 조정
5. 반복하여 최적의 모델 완성

### $D \to \mathcal{F} \to f$ 관점으로 본 학습 (p.33)

데이터가 제시한 문제를, 가설공간이 제공한 선택지 중에서, 가장 잘 푸는 함수를 찾는 것이 학습.

1. **데이터 $D$가 문제를 제시**
   - 교육 연수가 12년이면 수입 2200만원, 16년이면 수입 3500만원…
   - 위와 같은 "입력-출력" 관계를 보여주는 데이터들
2. **가설공간 $\mathcal{F}$가 선택지를 제공**
   - 예) "$y = ax + b$" 형태의 모든 1차 함수를 제시
   - 무수히 많은 후보 함수들의 집합
3. **학습이 최적의 $f$를 선택**
   - $\mathcal{F}$ 내 후보 함수들을 데이터를 활용하여 테스트함(예측 오차 계산)
   - 예측 오차가 가장 작은 함수 하나를 찾아냄

> **[그림]** — 좌측 "학습 전"은 x축 Years of Education(10~22), y축 Income(20~80)의 빨간 산점도만 있는 상태. 가운데 "$\mathcal{F}$: 선형함수 가설공간" 박스에는 같은 산점도 위에 여러 개의 회색 후보 직선들이 겹쳐 그려져 있음. 우측 "학습 후"는 그 중 선택된 파란 직선 하나가 데이터를 관통하는 모습으로, 후보 집합에서 하나를 고르는 것이 학습임을 보여줌.

## 4-1. 2D 피쳐 기반 학습 (p.36-38)

### 미지의 참 함수와 관측 가능한 것 (p.36)

$$\text{Income} = f^{*}(\text{Years of Education},\ \text{Seniority}) + \epsilon$$

- $f^{*}$ = 미지의 참 함수(입력과 출력을 이어주는 숨겨진 진짜 함수)
- 파란색 Surface(=미지의 참 함수 $f$)는 관측 불가능
- 빨간색 점들(=데이터)만 관측 가능함
- 학습 전: 어떤 가설공간 $\mathcal{F}$을 사용할까?
- 학습 후: 데이터를 활용하여 어떤 모델 $f$을 선택해야 할까?

> **[그림4-1] 학력과 수입 데이터** — 축이 Years of Education, Seniority, Income인 3D 플롯. 물결 모양의 파란 격자 곡면이 "미지의 참 함수 $f^{*}$"로 표시되고, 빨간 점들이 곡면 위아래로 흩어져 있으며 한 점에서 곡면까지의 수직 거리에 $\epsilon_i$가 표기됨.

### 가설공간의 선택: 선형 vs 비선형 (p.37-38)

3개의 3D 플롯(축: Years of Education, Seniority, Income)이 나란히 제시됨. 노란 곡면이 "모델", 빨간 점이 "데이터".

- 왼쪽 플롯: 평평한 평면 형태 → $\mathcal{F}$: 선형함수 가설공간
- 가운데·오른쪽 플롯: 점점 더 굽이치는 곡면 형태 → $\mathcal{F}$: 비선형함수 가설공간

같은 데이터라도 어떤 가설공간을 쓰느냐에 따라 선택되는 모델의 모양이 달라짐을 보여줌.

## 4-2. 일반적 용어 정리 및 모델 가정 (p.40)

$$\text{Income} = f^{*}(\text{Years of Education},\ \text{Seniority}, \dots) + \epsilon \;\rightarrow\; Y = f^{*}(X) + \epsilon$$

- Income: 우리가 예측하려는 라벨(반응/목표) 변수 → $Y$로 표기
- Years of Education: 첫번째 피쳐(입력/예측) 변수 → $X_1$로 표기
- Seniority: 두번째 피쳐(입력/예측) 변수 → $X_2$로 표기
- 다른 $i$번째 피처가 있다면 역시 $X_i$로 표기
- 일반적인 $p$차원 피쳐(총 $p$개의 피쳐) 벡터:

$$\boldsymbol{X} = \begin{bmatrix} X_1 \\ X_2 \\ \vdots \\ X_p \end{bmatrix} \in \mathbb{R}^p$$

- 모델(함수형): $f^{*}: \mathbb{R}^p \longrightarrow \mathbb{R}$ , $Y = f^{*}(\boldsymbol{X}) + \epsilon$
- 측정오차 $\epsilon$ : $\epsilon$는 피쳐 $\boldsymbol{X}$와 독립 및 $\mathbb{E}[\epsilon] = 0$로 가정함

## 4-3. 왜 $f(\cdot)$를 학습하는가? (p.42)

- **예측**: 잘 학습된 $f$가 있으면, 새로운 입력 $X = x$에서 반응/목표 $Y$를 예측할 수 있음
- **중요 특성 파악**: 피쳐들 $\boldsymbol{X} = (X_1, X_2, \dots, X_p)$ 중 어떤 특성이 $Y$를 설명하는데 중요하고, 어떤 것은 덜 중요(무관)한지 알 수 있음
  - 예: 근속 연수(Seniority), 교육 연수(Years of Education)는 소득(Income)에 큰 영향을 줄 수 있지만, 혼인 여부(Marital Status)는 영향이 거의 없을 것임
- **해석 가능성**: $f$의 복잡도에 따라 각 구성요소 $X_i$가 $Y$에 어떻게 영향을 미치는지(증가/감소 방향, 민감도 등) 이해할 수 있음

## 확인문제 (1차시)

### 문제 1. AI, ML, DL의 관계 (p.43-44)

다음 중 AI, ML, DL의 관계에 대한 설명으로 옳은 것은?

① ML은 AI과 DL의 상위 개념이다.
② AI는 반드시 ML을 사용해야 한다.
③ ML은 AI의 한 부분으로, 데이터로부터 학습을 수행한다.
④ 규칙 기반 시스템은 ML의 한 분야이다.
⑤ ML은 사람이 직접 코딩한 규칙을 그대로 사용하는 방식이다.

**정답: ③**
해설: ML은 AI의 한 분야로, 데이터로부터 규칙을 학습해 목표 달성을 돕는다.
① ML은 AI의 하위 개념이다. ② AI는 규칙/휴리스틱 기반만으로도 구현 가능하다. ④ 규칙 기반 시스템은 AI의 대표적인 예시이다. ⑤ ML은 데이터에서 추론한다.

### 문제 2. Feature와 Label (p.45-46)

다음 중 Feature와 Label에 대한 설명으로 옳지 않은 것은?

① Feature는 모델이 예측에 사용하는 입력 정보이다.
② Label은 모델이 예측하려는 정답, 즉 학습의 목표이다.
③ Feature와 Label의 분포와 관계가 학습 결과를 결정한다.
④ Feature는 반드시 수치형 데이터만 될 수 있다.
⑤ Label은 분류 문제에서는 카테고리, 회귀 문제에서는 수치값이 될 수 있다.

**정답: ④**
해설: Feature는 수치형뿐 아니라 범주형, 텍스트, 이미지/음성 등도 가능하다.

### 문제 3. $f(X)$를 학습하는 이유 (p.47-48)

다음 중 $f(X)$, 즉 미지의 참 함수를 학습하는 이유로 적절하지 않은 것은?

① Feature($X$)와 label($Y$) 사이의 관계를 알기 위해
② 모든 데이터의 측정오차($\epsilon$)를 제거하기 위해
③ 새로운 입력 $X$에 대해 목표값 $Y$를 예측하기 위해
④ 여러 feature($X$)들 중 label($Y$)와 관계를 갖고 있는 feature를 알기 위해
⑤ Feature($X$) 값의 변화에 따라서 label($Y$)이 어떻게 변화하는지 알기 위해

**정답: ②**
해설: 학습을 잘 한다고 하여서 측정오차를 줄이거나 제거할 수 없다.

## 강의 정리 (1차시) (p.49-50)

**AI와 ML**
- AI: 주어진 환경/데이터를 인지·학습·추론하여 목표 달성을 위한 예측·행동 선택·계획을 수행하는 시스템
- ML: AI 범주 내에서 데이터로부터 학습하여 목적을 달성하는 방법론

**데이터의 구성 요소**
- Feature: 모델 입력 정보, 예측·판단의 근거
- Label: 모델이 예측하려는 정답, 학습의 목표

**1D 피쳐 기반 학습**
- Feature가 하나일 때(1차원) 수행되는 단순한 학습 형태
- Feature와 Label 사이의 관계를 함수 $f$로 표현하되, 실제 데이터에는 측정오차 $(\epsilon)$가 포함됨

**모델과 가설 공간**
- 가설 공간($\mathcal{F}$): Feature와 Label 관계를 표현할 수 있는 함수들의 집합
- 모델: 가설 공간 내 특정 함수 $f$

**학습**
- 데이터와 성능 척도를 바탕으로 가설 공간 $\mathcal{F}$ 내 후보 중 최적의 모델을 선택하는 과정

**모델 학습의 필요성**
- 예측: 새로운 입력값에 대해 Label을 추론
- 중요 특성 파악: 어떤 Feature가 결과에 중요한지 확인
- 해석 가능성: 각 Feature가 결과에 어떤 영향을 미치는지 이해

---

# 2차시 — AI & 기계학습 기초 2: 지도학습은 무엇인가?

## 강의 목차 (p.52)

1. 지도학습의 개념 — 지도학습의 정의와 특징 / 지도학습의 종류 (회귀 vs 분류) / 지도학습 용어
2. 회귀(Regression) — 회귀의 정의와 예시 / MSE / $R^2$
3. 분류(Classification) — 분류의 정의와 특징 / 분류 정확도(Accuracy) / 혼동행렬
4. 학습의 목적 — 학습의 목적 / 오버피팅(Overfitting)

## 학습 목표 (p.53)

- 회귀(연속값)와 분류(범주값)의 문제 정의와 출력 차이 이해
- 문제 유형에 맞는 오류/평가지표를 바르게 선택·해석
- 학습의 목적: 테스트 성능 최대화(테스트 오류 최소화)에 대한 이해
- 오버피팅(overfitting)의 이해

## 학습 시작(Overview): "처음 보는 문제도 잘 푸는 AI 만들기" (p.55-56)

- 정의: 입력(feature) + 정답(label)을 가지고 예측 규칙을 배우는 방법
  - 정답이 있는 예시 데이터로 AI를 훈련시켜서, 새로운 문제에도 정확한 답을 할 수 있게 만드는 방법
- 목표: 훈련 데이터 뿐만 아니라, 처음 보는 데이터에서도 예측 성능 향상

**예시 1) 어제까지 고객 데이터로 "내일 이탈할 고객" 미리 알기**
- 기존 고객 정보 + 탈퇴 여부 → 내일 이탈할 고객 예측

**예시 2) 기존 거래 사기(Fraud) 데이터로 새로운 사기 탐지**
- 기존 사기 거래 데이터 → 사기인지 아닌지 구분하지 못했던 거래 or 새로운 사기거래 예측

> **[그림0-1] 고객 이탈 예측** — 왼쪽 "훈련 데이터"(사람 아이콘 묶음) → 가운데 "지도 학습"(차트와 돋보기 아이콘) → 오른쪽 "데이터 예측"(문을 나가는 사람과 경고 표시 아이콘)으로 이어지는 흐름도.

## 1. 지도학습의 개념

### 지도학습(Supervised learning)이란? (p.58-59)

- 정답 라벨이 있는 훈련 데이터를 사용해 예측 모델을 학습하는 방법
- 목표: 훈련 데이터로 학습한 모델이 새로운 데이터에서도 정확한 예측을 수행
  - 데이터: 입력(feature, $x$)과 정답(label, $y$)이 항상 쌍으로 존재
  - 데이터들은 여러 개 축적 돼 있음

> **[그림1-1] 지도 학습의 예** — 왼쪽 "훈련 데이터(Train Set)"의 고양이·개 사진들에 각각 "고양이", "개" 정답 라벨이 붙는 "레이블링" 단계 → "신경망 등 알고리즘"으로 "학습" → "개·고양이 분류모델" 생성 및 적용 → 위쪽의 "테스트 데이터(Test Set)" 사진들이 모델을 거쳐 오른쪽 "결과 확인"에서 각각 고양이/개로 분류됨(새로운 데이터에서도 정답을 맞춤).
> 출처: https://ellun.tistory.com/103

### 지도학습의 종류: 회귀(Regression) vs 분류(Classification) (p.60-61)

- **회귀(Regression)**: 예측하고 싶은 결과값이 **숫자**일 때 사용. 예시) 가격, 점수, 온도
- **분류(Classification)**: 예측하고 싶은 결과값이 **범주**일 때 사용. 예시) 스팸/정상, 질병 유/무

회귀 예시 — 공부시간과 시험점수 (AI가 예측해야 할 값이 "숫자")

| 공부 시간 | 시험 점수 |
|---|---|
| 1시간 | 10점 |
| 2시간 | 20점 |
| 3시간 | 30점 |
| 4시간 | ?점 |

> **[그림]** 분류 예시 두 가지 — (1) "스팸/정상 분류": 정상 메일 아이콘과 스팸 메일 아이콘을 보여준 뒤 새 메일에 대해 "???"를 예측하게 하는 그림. (2) "고양이/강아지/햄스터 분류": 고양이·강아지·햄스터 사진을 학습하고 새 고양이 사진에 "???"를 예측하는 그림. 두 경우 모두 AI가 예측해야 할 값이 "범주"임을 보여줌.

### 손실 함수(Loss Function) (p.62)

- 모델의 예측이 정답에서 얼마나 벗어났는지를 하나의 숫자로 나타내는 함수
- 학습이란, 이 숫자가 작아지는 방향으로 모델을 조금씩 고쳐 나가는 과정

**문제 유형에 따라 쓰는 손실이 다르다**

- **회귀 → 평균제곱오차 (MSE)**: $\mathrm{MSE} = \frac{1}{n} \sum (y - \hat{y})^2$ — 정답과 예측의 차이를 제곱해서 평균낸 값. 많이 틀릴수록 제곱 때문에 손실이 크게 커짐
- **분류 → 교차 엔트로피 (Cross Entropy)**: $\mathrm{CE} = -\sum y \times \log(\hat{y})$ — 정답 범주에 얼마나 높은 확률을 주었는지 측정. 정답을 확신할수록 0에 가까워짐

※ MSE는 2장(회귀), 교차 엔트로피는 3장(분류)에서 자세히 다룸.

### 지도학습 용어 (p.63)

- **특성(Feature, $x$)**: 예측에 사용되는 설명 변수. 예측 함수 $f$의 input으로 들어가는 변수
  - 예: 집값 예측에서의 feature {지역, 평수, 방수, 연식}, 이메일 스팸 필터링에서의 feature {제목, 내용 텍스트, 송신인}
- **라벨(Label, $y$)**: 맞춰야 하는 정답(실제값). 예: 집값, 스팸/정상이메일
- **예측값($\hat{y}$)**: 학습 과정에서 모델이 내놓은 결과(숫자 또는 범주). $y$의 추정치
- **오류(Error)**: 손실 함수로 계산. 예측값($\hat{y}$)과 라벨($y$)의 차이: $\hat{y} - y$
  - 오류 ≠ 측정 오차. 예측값과 실제값의 차이(오류)를 줄이는 것이 지도학습의 목표!

> **[그림1-2] 집값 예측** — x축이 "지역명"(서초구, 강남구, 송파구, 과천시, 용산구, 분당구, 강동구, 동작구, 수정구, 서대문구), y축이 "평균가(억)"(0.0~40.0)인 그래프. "2025년 25평 아파트 기준". 검은 점들이 실제 데이터이고 초록색 직선이 "예측값", 점과 직선을 잇는 빨간 화살표가 "오류"로 표시됨. x축 영역 전체가 "특성", y축 영역이 "라벨"로 표시되어 있음.

## 2. 회귀(Regression)

### 회귀 문제의 정의 (p.65-66)

- **정의**: 입력(feature)으로부터 숫자(output)를 얼마나 정확히 예측할까?
- **특징**: 라벨 및 예측 모델의 출력은 **연속적인 수치**

> **[그림2-1] 회귀 예시** — x축 20~100, y축 0~100의 산점도에 주황색 점들이 우상향으로 흩어져 있고 그 사이를 관통하는 검은 직선이 그려져 있으며, 아래에 "Regression"이라고 적혀 있음.

**예시 1) 집값 예측** — Feature: 면적, 방 수, 연식 / Label: 집값

| 면적($m^2$) | 방 수 | 연식 | 집 값(원) |
|---|---|---|---|
| 85 | 3 | 5 | 4억2천 |
| 102 | 4 | 2 | 5억8천 |
| 70 | 2 | 10 | 3억5천 |
| 90 | 3 | 3 | ? |

**예시 2) 매출액 예측** — Feature: 매체 별 광고비 / Label: 매출액

| TV 광고비 | 라디오 광고비 | 인터넷 광고비 | 매출액 |
|---|---|---|---|
| 200 | 50 | 100 | 8억5천 |
| 150 | 80 | 150 | 7억2천 |
| 300 | 30 | 80 | 9억1천 |
| 180 | 40 | 90 | ? |

### 회귀 오류: 평균제곱오차(MSE) (p.67-68)

$$MSE = \frac{1}{n}\sum_{i=1}^{n}(y_i - \hat{y}_i)^2$$

- 각 데이터에서 정답($y_i$)과 예측($\hat{y}_i$)의 평균 제곱 차이값
- 틀린 정도를 제곱해서 더 큰 오류에 패널티 부여 → 전체 평균 오류 수준을 한눈에 볼 수 있음
- 오류가 작을수록 정확한 예측 → 오류를 줄이는 것이 목표!

**참고: MSE 단위가 제곱이라 해석이 어렵다면?** 원래 데이터와 같은 단위라서 해석이 쉬운 **RMSE**(MSE의 제곱근)도 사용 (R은 root를 의미).

$$\mathrm{RMSE} = \sqrt{\mathrm{MSE}} = \sqrt{\frac{1}{n}\sum_{i=1}^{n}(y_i - \hat{y}_i)^2}$$

> **[그림2-2] MSE 설명 그래프** — 가로축 $X$, 세로축 $Y$의 산점도에 하늘색 직선 "예측 모델 $f$"가 그려져 있음. 특정 $X_i$ 위치에서 "실제 값 $y_i$"와 "예측 값 $\hat{y}_i$"를 점선으로 표시하고, 그 간격을 $e_i = y_i - \hat{y}_i$ (실제 값 − 예측 값) 오류 $e$로 표기.

### 회귀 설명력: $R^2$ (결정계수) (p.69-70)

- Label($y$)의 분산 중에서 Feature로 설명되는 비율
- "평균만 쓰는 단순한 예측"보다 모델이 실제값을 얼마나 더 잘 맞추는지를 0~1 사이 값으로 나타냄
- 1에 가까울수록 설명력이 높고, 낮을수록 설명력이 낮음

$$R^2 = 1 - \frac{\sum_{i=1}^{n}(y_i - \hat{y}_i)^2}{\sum_{i=1}^{n}(y_i - \bar{y})^2}$$

여기서 $\bar{y} = y_i$들의 평균값

> **[그림2-3] $R^2$ 설명 그래프** — 가로축 X, 세로축 Y. 빨간 우상향 직선이 모델 예측값 $\hat{y}$, 파란 수평선이 실제값들의 평균값 $\bar{y}$. 한 데이터점에서 노란 점 $y_i$(실제값), 주황 점 $\hat{y}_i$, 파란 점 $\bar{y}$를 표시하고, $y_i$와 $\bar{y}$의 간격에 $\sum(y_i-\bar{y})^2$(분모), $y_i$와 $\hat{y}_i$의 간격에 $\sum(y_i-\hat{y}_i)^2$(분자)를 대응시켜 보여줌.

**질문: $R^2$가 음수가 나올 수 있을까?** → 나올 수 있음. 예측값($\hat{y}_i$)들이 평균값 $\bar{y}$보다도 못하다면…

### MSE vs $R^2$ (p.71-74)

MSE, $R^2$ 둘 다 모델의 정확도를 평가하기 위한 정량 지표.

| 항목 | MSE | $R^2$ |
|---|---|---|
| 정의 | 예측값과 실제값 차이의 제곱 평균 | 평균 예측 대비 모델의 상대적 성능 |
| 값 범위 | 0 ~ ∞ (무한대) | 0 ~ 1 |
| 단위 | 실제 데이터 단위² | 단위 없음 |
| 해석 | 절댓값 (구체적 오차 크기) | 상대적 비율 (성능 정도) |
| 좋은 값 | 0에 가까울수록 좋음 | 1에 가까울수록 좋음 |
| 장점 | 구체적 오차 크기 알 수 있음 | 직관적 해석, 모델 간 비교 쉬움 |
| 단점 | 직관적 해석 어려움 | 구체적 오차 크기 모름 |

**MSE = $(1000만원)^2$은 좋은 성능의 모델일까?**

"평균적으로 1000만원 정도 실제값과 차이가 있다"(MSE) VS "모델이 데이터 변동의 80%를 설명한다"($R^2$)

- MSE = (1000만원)$^2$ : 모델이 예측한 값이 실제값과 평균적으로 1000만원 정도 오류가 있다
- $R^2$ = 0.8 : 평균값만으로 예측할 때 발생하는 제곱오차의 80%를 줄였다
- 집값 예측에서 MSE = (1000만원)²라면 좋은지 나쁜지 애매하지만, $R^2$ = 0.8이라면 "꽤 좋은 모델"이라는 판단 가능

**각 지표는 언제 사용하면 좋을까?**

- **MSE**: 구체적인 오차 크기가 중요할 때 / 실제 손실 비용을 계산할 때
- **$R^2$**: 모델 성능을 직관적으로 평가할 때 / 여러 모델을 비교할 때 / 모델이 전반적으로 얼마나 좋을지 알고 싶을 때

→ 필요에 따라 상호 보완적으로 함께 사용하자!

## 3. 분류(Classification)

### 분류 문제의 정의 (p.76-79)

- **정의**: 입력 특성(Feature)으로부터 출력이 범주값(카테고리)으로 나타나는 예측 문제
- **특징**: 범주 라벨(이진/다중) — 분류 모델에서 라벨은 미리 정해진 범주(카테고리) 중 하나의 값을 가짐

**이중 라벨 예시 1) 스팸 메일 분류** — Feature: 내용, 메일주소, 작성자… / Label: 스팸/정상

| 내용 | 메일 주소 | 작성자 | 스팸/정상 |
|---|---|---|---|
| "무료 대출!" | promo@loan.com | 대출왕 | 스팸 |
| "회의 자료…" | kim@company.com | 김대리 | 정상 |
| "1억 당첨!" | winner@lucky.net | 이벤트팀 | 스팸 |
| "교육 지원금…" | ssafy@multicampus.com | 프로님 | ? |

**이중 라벨 예시 2) 의학 예시** — Feature: 종양 반경, 면적, 색상… / Label: 악성/양성

| 종양 반경(mm) | 면적($mm^2$) | 색상 | 악성/양성 |
|---|---|---|---|
| 15 | 180 | 검은색 | 악성 |
| 8 | 50 | 갈색 | 양성 |
| 22 | 380 | 불규칙 | 악성 |
| 12 | 110 | 갈색 | ? |

**다중 라벨 예시) 학점 예측** — Feature: 출석률, 과제 점수, 중간고사 점수 / Label: 학점(A/B/C)

| 출석률(%) | 과제 점수 | 중간고사 점수 | 학점 |
|---|---|---|---|
| 95 | 85 | 90 | A |
| 70 | 60 | 65 | C |
| 88 | 78 | 82 | B |
| 85 | 75 | 80 | ? |

> **[그림3-1] 분류** — x축 -0.5~0.2, y축 -0.6~0.4의 산점도에 연두색 점들과 청록색 점들이 섞여 있고, 이를 가르는 구불구불한 검은 곡선이 그려져 있으며 아래에 "Classification"이라고 적혀 있음.

- **Decision Boundary**: 서로 다른 클래스(범주)를 구분하는 경계선. 분류모델은 이 선을 기준으로 데이터를 구분

### 분류 정확도(Accuracy) (p.80-81)

분류모델은 Label이 범주로 돼 있기 때문에 MSE나 $R^2$를 사용하기 어렵다.

전체 예측 중에서 올바르게 **맞춘 예측의 비율**

$$\text{Accuracy} = \frac{1}{n}\sum_{i=1}^{n}\mathbb{I}(y_i = \hat{y}_i)$$

$$Accuracy = \frac{\text{올바르게 맞춘 개수}}{\text{전체 예측 개수}}$$

- $\mathbb{I}$ : 지시(indicator) 함수, $\mathbb{I}(A) = 1$ if $A$ true
- 지시함수($\mathbb{I}$): 조건이 참이면 1, 거짓이면 0을 반환하는 함수
  - $\mathbb{I}$(조건) = 1 if 조건이 참
  - $\mathbb{I}$(조건) = 0 if 조건이 거짓
  - $\mathbb{I}(y_i = \hat{y}_i)$ = 1 if 실제값 = 예측값 (맞춤)
  - $\mathbb{I}(y_i \neq \hat{y}_i)$ = 0 if 실제값 ≠ 예측값 (틀림)

**정확도 예시: 고양이 분류**

- 실제($y_i$): 고양이, 예측($\hat{y}_i$): 고양이 → $\mathbb{I}$(고양이 = 고양이) = 1 (맞춤)
- 실제($y_i$): 고양이, 예측($\hat{y}_i$): 강아지 → $\mathbb{I}$(고양이 ≠ 강아지) = 0 (틀림)

### 정확도의 함정: 불균형 데이터 문제 (p.82-83)

정확도가 99%이면 좋은 모델일까? → 불균형 데이터(양성 1%, 음성 99%)에서는 **전부 음성**이라 해도 정확도가 99%로 보일 수 있음

- 질병 진단 데이터 1000명 / 실제 질병 환자: 10명 (1%) / 실제 건강한 사람: 990명 (99%)
- → 무조건 모든 사람을 "건강하다"고 예측하는 문제가 있는 모델
- → 질병 환자 → "건강"으로 예측(10개 틀림), 건강한 환자 → "건강"으로 예측(990개 맞음)

$$\text{정확도} = \frac{\text{맞춘 개수}}{\text{전체 개수}} = 99\%$$

- 정확도 99%지만 질병 환자를 전부 놓침
- 실제로 중요한 "질병 발견" 능력이 전혀 없음

**결론**: 정확도는 단순히 "전체 중 맞춘 비율"만 계산함. 정확도만 보지 말고 **다른 지표**도 함께 봐야 안전. 예) 혼동행렬

### 혼동행렬(Confusion Matrix) (p.84-85)

- 예측과 실제 값 사이의 관계를 행렬 형태로 표현
- **TP**: 실제 양성, 예측도 양성
- **TN**: 실제 음성, 예측도 음성
- **FP**: 실제는 음성인데 양성이라 함(**오탐**)
- **FN**: 실제는 양성인데 음성이라 함(**누락**)

> **[그림3-2] 혼동행렬** — 행이 Actual Class(Positive/Negative), 열이 Predicted Class(Positive/Negative)인 2×2 표.

| Actual \ Predicted | Positive | Negative | |
|---|---|---|---|
| **Positive** | True Positive (TP) | False Negative (FN) / Type II Error | Sensitivity $\frac{TP}{TP+FN}$ |
| **Negative** | False Positive (FP) / Type I Error | True Negative (TN) | Specificity $\frac{TN}{TN+FP}$ |
| | Precision $\frac{TP}{TP+FP}$ | Negative Predictive Value $\frac{TN}{TN+FN}$ | Accuracy $\frac{TP+TN}{TP+TN+FP+FN}$ |

- **정밀도(Precision)**: "양성이라 **판정한 것 중**" 진짜 양성의 비율 = TP/(TP+FP)
- **재현율(Sensitivity or Recall)**: "진짜 양성 **가운데**" 잡아낸 예측 양성 비율 = TP/(TP+FN)
- **F1-score**: 정밀도와 재현율의 조화평균

$$\mathbf{F1} = 2 \times \frac{\text{정밀도} \times \text{재현율}}{\text{정밀도} + \text{재현율}}$$

### 분류 손실: 교차 엔트로피(Cross Entropy) (p.86)

**정확도만 보고 학습할 수 있을까?**

- 정확도·혼동행렬은 학습이 끝난 결과를 "평가"하는 지표
- 맞음/틀림만 세므로, 예측 확률이 0.51에서 0.95로 좋아져도 값은 그대로임
- 학습 중에는 "정답을 얼마나 확신했는지"까지 반영하는 손실이 필요

**교차 엔트로피 (Cross Entropy)**

- 정답에 준 확률이 높을수록 손실이 작아짐
- 확률이 낮으면 손실이 급격히 커짐

$$\text{손실} = -\log(\text{정답 범주에 준 확률})$$

- 회귀의 MSE에 해당하는, 분류의 대표 손실 함수

**[표3-1] 예측 확률에 따른 교차 엔트로피 손실**

| 실제 정답 | 스팸일 확률 예측 | 손실 $-\log(p)$ |
|---|---|---|
| 스팸 | 0.9 | 0.11 (작음) |
| 스팸 | 0.6 | 0.51 |
| 스팸 | 0.1 | 2.30 (큼) |

※ 정확도·정밀도·재현율 = 결과를 "평가"하는 지표 / 교차 엔트로피 = 학습에서 "줄여 나가는" 손실

## 4. 학습의 목적

### 학습의 목적: 새로운 데이터에서 예측하기 (p.88-89)

목적: 훈련 데이터로 학습한 모델이 새로운 데이터에서도 정확한 예측을 수행하도록 하는 것

**훈련이 잘 됐는지 어떻게 확인할까? (모델 성능을 어떻게 평가할까?)**

- 학습 모델의 성능 평가는 **모델이 처음 보는(학습에 사용되지 않은)** 데이터로 평가
  - "이 모델이 훈련 상황 외에 새로운 상황에서도 잘 동작할까?"(일반화)를 검증
- 훈련 데이터에서 성능이 아무리 좋아도, 새로운 데이터에서 성능이 떨어지면 **실전엔 사용할 수 없음**
  - **일반화(generalization)** 오류의 최소화 지향
  - → 새로운 데이터에서 틀리는 걸 최대한 줄여야 한다!
- 다음 차시(3차시)에서 **일반화 성능을 추정**(검증/교차검증)하는 방법을 배울 예정

### 오버피팅(overfitting)이란? (p.90-91)

- 훈련 데이터의 **우연한 패턴/잡음**까지 외워버려서 (초록색 함수) 훈련에서는 잘 맞지만 **테스트에서는 성능이 나빠지는** 현상
- 현상: **훈련 오류 급격히 낮음**, 테스트 오류 **높음/요동**

**오버피팅이 왜 안 좋은가?**

- **표본(sample) 의존·불안정**: 훈련 데이터는 모집단의 일부 표본이라 우연한 잡음이 섞임. 이것에만 과하게 맞추어 학습하면 **샘플 몇 개만 바뀌어도 예측이 크게 흔들림**(분산↑).
- **일반화 실패**: 보지 못한 데이터(테스트) 오류가 커짐, 모집단(population) 성능과 격차가 벌어짐.

> **[그림4-1] 오버피팅** — 2차원 평면에 빨간 점과 파란 점이 섞여 분포하고, 두 개의 경계선이 그려져 있음. 검은색 매끄러운 곡선은 두 클래스를 완만하게 가르고, 초록색 곡선은 개별 점 하나하나를 감싸려고 심하게 구불거리며 튀어나옴 — 이 초록색 함수가 잡음까지 외운 오버피팅된 결정경계.

### 오버피팅에 대한 오해: 분포 변화(distribution shift) (p.92)

**오버피팅 ≠ 분포 변화(distribution shift)로 인한 에러 증가**

- **분포 변화로 인한 오류**: 훈련 데이터 분포와 테스트 **분포가 다름으로**(환경·계절·센서 변경 등) 성능이 떨어지는 현상
- **분포 변화**로 인한 에러 증가는 **모델이 과적합하지 않아도** 발생 가능

> **[그림]** 상단 — train 산점도와 test 산점도를 vs로 비교. 두 그림 모두 빨간 x와 초록 x가 있고 보라색 점선 결정경계가 그려져 있는데, train에서는 경계가 우상향 대각선, test에서는 데이터 배치가 달라져 경계가 거의 수평으로 다르게 놓임. 하단 — 훈련 데이터는 cat/cat/dog/dog의 실제 사진, 테스트 데이터는 같은 cat/cat/dog/dog 라벨이지만 만화 일러스트 이미지. 라벨은 같으나 입력 분포가 바뀐 예.

### 오버피팅 vs 언더피팅 (p.93)

- **오버피팅**: 모델이 너무 복잡 → 잡음까지 학습(테스트 성능 나쁨)
- **언더피팅**: 모델이 단순하거나 학습이 완료되지 않음 → 중요한 패턴을 놓침(오류 큼)

**해결 실마리**: 더 많은 데이터, 테스트 데이터를 활용한 모델 선정, 교차 검증(3차시)

> **[그림4-2] 오버피팅 vs 언더피팅** — 2행(분류/회귀) × 3열(Overfitting / Right Fit / Underfitting) 격자. 분류 행에서는 파란 점과 주황 점을 가르는 경계가 Overfitting에서 구불구불 뒤엉키고, Right Fit에서는 완만한 곡선, Underfitting에서는 단순한 직선이라 많은 점을 잘못 가름. 회귀 행에서는 주황 점들에 대해 Overfitting은 점마다 출렁이는 곡선, Right Fit은 부드러운 U자형 곡선, Underfitting은 거의 평평한 직선을 그림.

## 확인문제 (2차시)

### 문제 1. 지도학습(supervised learning) (p.94-95)

다음 중 지도학습(supervised learning)에 대한 설명으로 옳은 것은?

① 입력 데이터만 있고 정답(라벨)이 없는 데이터로 학습한다.
② 새로운 입력에 대해 정답을 잘 맞추는 함수를 학습한다.
③ 오버피팅은 훈련데이터에서의 오류를 최대한 줄이는 것으로 학습이 목표하는 방향이다.
④ 지도학습 모델의 출력은 반드시 범주(카테고리)이다.
⑤ 지도학습은 측정오차를 최소화하는 것이 목적이다.

**정답: ②**
해설: 지도학습은 입력(특성)과 정답(라벨)이 쌍으로 있는 데이터를 학습하여 새로운 입력의 정답을 맞추는 규칙을 찾는다.
① 입력만 있고 정답이 없는 경우는 비지도 학습이다. ③ 일반화 성능을 높이는 것이 궁극적인 목적이기에 오버피팅은 피해야 한다. ④ 지도학습 모델 출력은 수치(회귀문제)와 범주(분류문제) 모두 가능하다. ⑤ 지도학습에서는 예측값과 정답의 차이(오차)를 줄이는 것이 핵심이다.

### 문제 2. 회귀(Regression) 문제의 특징 (p.96-97)

다음 중 회귀(Regression) 문제의 특징으로 옳은 것은?

① 스팸/정상 분류와 같은 범주 예측 문제를 다룬다.
② 예측값은 연속적인 숫자(예: 가격, 점수, 온도)로 나온다.
③ 대표적인 성능 지표는 정확도(Accuracy)이다.
④ 예측 결과는 이산적인 값으로 한정된다.
⑤ 훈련 데이터와 테스트 데이터의 분포가 다르면 반드시 발생한다.

**정답: ②**
해설: 회귀 문제는 연속적인 수치를 예측하는 문제다. 예를 들어 집값, 매출액, 온도 등이 있다.
① 범주를 예측하는 것은 분류(Classification) 문제다. ③ 회귀에서는 MSE, RMSE, $R^2$ 등의 지표를 사용한다. ④ 이산 값은 분류 문제의 특징이다. ⑤ 훈련/테스트 분포 차이는 데이터 쉬프트 개념이지 회귀의 특징은 아니다.

### 문제 3. 혼동행렬 (p.98-99)

다음 중 혼동행렬에 대한 설명으로 옳지 않은 것은?

① TP(True Positive): 실제 양성을 양성으로 예측한 경우
② TN(True Negative): 실제 음성을 음성으로 예측한 경우
③ FP(False Positive): 실제 음성인데 양성으로 잘못 예측한 경우
④ FN(False Negative): 실제 양성인데 음성으로 잘못 예측한 경우
⑤ 혼동행렬은 회귀 문제의 성능 평가에서 사용된다.

**정답: ⑤**
해설: 혼동행렬은 분류 문제의 성능 평가 지표이다. 회귀 문제에서는 혼동행렬 대신 MSE, RMSE, $R^2$ 등의 지표를 사용한다.

## 강의 정리 (2차시) (p.100-103)

**지도학습(Supervised Learning)**
- 입력(Feature)과 정답(Label)이 쌍으로 있는 데이터로 학습
- 새로운 입력에 대해 정답을 잘 예측하는 규칙을 찾는 과정
- 종류: 회귀(Regression) 연속적인 값 예측(집값, 점수, 매출액) / 분류(Classification) 범주 예측(스팸/정상, 질병 유무)

**평가 지표**
- 회귀: MSE(평균제곱오차) 큰 오류를 더 크게 반영해 전체 오류 수준을 파악 / $R^2$(결정계수) 모델이 데이터를 얼마나 설명하는지 (0~1 사이, 높을수록 설명력 큼)
- 분류: 정확도(Accuracy) 전체 중 맞춘 비율(불균형 데이터에서 한계 존재) / 혼동행렬(Confusion Matrix) 예측 vs 실제를 행렬로 표현

**학습의 목적**
- 일반화(Generalization): 처음 보는 데이터에서도 잘 작동해야 함
- 훈련 데이터 성능만 좋고 실제 성능이 낮으면 활용 불가

**오버피팅(Overfitting)**: 훈련 데이터의 잡음까지 학습 → 훈련 성능↑, 테스트 성능↓ / 훈련 오류 작음, 테스트 오류 큼 / 모델이 너무 복잡 → 과적합

**언더피팅(Underfitting)**: 모델이 너무 단순하거나 학습이 부족 → 중요한 패턴을 놓침 / 훈련·테스트 모두 오류 큼 / 모델이 너무 단순 → 과소적합

**균형 잡기 해결 방법**: 더 많은 데이터 확보, 테스트 데이터를 활용한 모델 선정, 교차 검증

---

# 3차시 — AI & 기계학습 기초 3: 교차검증(Cross-Validation)

## 목차 (p.105)

1. 테스트 성능 평가
2. 검증셋(validation set) 접근
3. K-겹 교차검증 (K-fold Cross-Validation)

## 학습 목표 (p.106)

- 학습 데이터와 새로운 데이터에서 일어나는 오류의 차이를 설명할 수 있다.
- '처음 보는 데이터 예측(일반화)'이 왜 중요한지 설명할 수 있다.
- 검증셋(hold-out)과 교차검증(K-fold)이 무엇인지 알고, 각각의 장단점을 설명할 수 있다.

## 1. 테스트 성능 평가

### 훈련 오류 vs 테스트 오류 (p.110-111)

- **훈련 오류**: 모델을 학습시킨 **같은 데이터**에 다시 적용해 계산한 오류
- **테스트 오류**: 학습에 쓰지 않은 **새 관측치**에 대해 모델을 적용했을 때의 평균 예측오류
  - 훈련 오류 = 문제집 점수, 테스트 오류 = 시험 점수 | 문제집만 잘 보면 시험을 잘 본다고 장담할 수 없음!
- 보통 훈련 오류는 테스트 오류와 다르며, 특히 훈련 오류는 테스트 오류를 (심하게) 과소평가하는 경우가 많음
  - 예: 암기 vs 응용 시험

> **[그림1-1] 훈련 오류와 테스트 오류** — 가운데 "Cat"이라 표시된 고양이 사진 묶음이 있고, 왼쪽 "훈련오차" 상자에는 그 안에 있던 고양이 한 장이 "Dog"로 예측된 모습, 오른쪽 "테스트오차" 상자에는 새로운 고양이 사진이 "Dog"로 예측된 모습이 각각 화살표로 연결되어 있음.

**훈련 오류·테스트 오류 곡선 (p.111)**

- **파란색 선**: 훈련 오류 / **빨간색 선**: 테스트 오류
- 훈련 오류는 계속 ↓, 테스트 오류는 U자형
- **목적**: 테스트 오류의 U자형의 바닥이 되도록 하는 적절한 모델 찾기
  - 너무 많이 답을 외우지도(과적합), 너무 대충 공부하지도 않은(과소적합) 딱 적당히 공부해서 점수가 가장 잘 나오는 지점 찾기

> **[그림1-2] 언더피팅·오버피팅 곡선** — 가로축 Model Complexity(Low → High), 세로축 Prediction Error. 파란 곡선(Training Sample)은 복잡도가 커질수록 단조 감소하고, 빨간 곡선(Test Sample)은 내려가다가 최저점을 지나 다시 올라가는 U자형. 왼쪽 상단에 "High Bias / Low Variance", 오른쪽 상단에 "Low Bias / High Variance" 표시.

### 테스트 예측 오류 계산과 재표본화(resampling) (p.112-113)

**테스트 예측 오류 계산**
- 이상적 케이스: 충분히 큰 별도 테스트 데이터셋 → 현실에선 구하기 어려움
- 현실에서는 테스트만을 위한 데이터를 갖기에 데이터 자체가 부족할 수 있음

**대안: 재표본화(resampling)를 통한 테스트 오류 추정**
- **데이터를 나눠** 여러 번 "훈련→평가"를 반복해 테스트 오류를 가늠
  - 시험용 문제가 따로 없다면?! ⇒ 기존 문제집의 문제들을 활용해 모의고사를 보자!
- 방법: 검증셋(hold-out), K겹 교차검증(K-fold Cross-Validation)
- 장점: 별도의 테스트 데이터 없이 **데이터를 더 효율적으로 사용**하여 일반화 오차 추정

## 2. 검증셋(Validation Set) 접근

### 검증셋(홀드아웃) 방법 (p.115-116)

- 가용 샘플들을 무작위로 **훈련셋**과 **검증셋(hold-out)**으로 분할
  - 문제집의 문제들을 모의고사용과 학습용으로 나누고 모의고사는 딱 1번만 보기
- 훈련셋으로 **모델 적합**, 검증셋으로 **예측 후 검증 오류**를 계산
- 검증 오류는 보통 **정량 반응**은 MSE, **범주 반응**은 **오분류율(또는 F1-score)**을 측정

> **[그림2-1] 홀드아웃** — 고양이 사진 묶음이 곡선 하나로 두 영역으로 갈라져 있고, 왼쪽 파란 영역은 "훈련셋", 오른쪽 주황 영역은 "검증셋"으로 표시.

**검증 절차 (p.116)**

- 데이터 순서 무작위 **셔플링** 후 **두 부분으로 분할**: 왼쪽(파랑)=훈련셋, 오른쪽(주황)=검증셋
- 학습은 **훈련셋**에서, 성능평가는 **검증셋**에서 수행

> **[그림2-2] 검증 절차** — 위쪽 흰 막대에 원래 데이터 순서 `1 2 3 … n`이 적혀 있고, 아래쪽 막대는 셔플된 인덱스 `7 22 13 … 91`이 파란 구간(훈련셋)과 주황 구간(검증셋)으로 나뉘어 있음.

### 검증셋 방법 예시: 자동차 데이터 (p.117)

- 목표: **선형**(1차)모델부터 **고차항**(다항식) 모델 비교
- 392개 데이터를 무작위로 196개 훈련셋 / 196개 검증셋으로 분할 (일반적으로 반으로 나눌 필요는 없음)
- **좌측 패널**: 단 한 번 분할 시의 MSE 곡선
- **우측 패널**: 여러 번 (셔플 후) 다른 분할의 MSE 곡선들

> **[그림2-3] 자동차 데이터** — 두 그래프 모두 가로축 Degree of Polynomial(1~10), 세로축 Mean Squared Error(약 16~28). 좌측은 빨간 점 하나의 곡선으로, degree 1에서 약 23.5였다가 degree 2에서 약 19로 급감한 뒤 완만하게 18 근처로 유지. 우측은 서로 다른 분할로 얻은 여러 색 곡선이 겹쳐 있으며, degree 2 이후 곡선들이 대략 16~25 사이로 넓게 흩어져 서로 다른 모양을 보임.

### 검증셋 접근의 한계 (p.118-119)

- 어떤 표본이 훈련/검증에 들어가느냐에 따라 **검증 기반 테스트 오류 추정치가 매우 가변적**
- 검증 접근에서는 훈련셋(=전체의 일부)만으로 모델을 **적합**하므로, 전체 데이터로 학습했을 때보다 **성능이 낮게 추정(즉, 테스트 오류를 과대 추정)**될 수 있음

**왜 전체 데이터로 학습한 모델의 테스트 오류를 과대 추정하는 경향이 있을까?**
- → 학습에 데이터를 부분만 사용하기 때문
- → 예) 훈련셋을 전체의 ½ 사용한 경우, 전체 데이터로 학습한 경우보다 예측 성능이 매우 낮을 것임

## 3. K-겹 교차검증(K-fold Cross-Validation)

### 개념과 절차 (p.121-122)

- 테스트 오류 추정의 **표준적** 접근
  - 문제집을 5등분 해서 서로 다른 모의고사를 5번 보는 것
- 추정치는 **모델 선택**과 **최종 모델의 테스트 오류 규모 파악**에 활용
- 데이터 전체를 **크기 동일한 $K$개 폴드**로 무작위 분할 → 폴드 $k \in \{1,2,\cdots,K\}$를 **검증**, 나머지 $K-1$개를 **훈련**에 사용
- $k=1,\cdots,K$에 대해 **반복** 후, **평균 오류**로 테스트 오류를 추정
- 데이터를 먼저 **셔플링**한 뒤, 총 $n$개의 데이터를 **겹치지 않는 $K$개 그룹으로 분할**
- 각 그룹이 번갈아 **검증셋(주황)**, 나머지는 **훈련셋(파랑)**

> **[그림3-1] K겹 교차검증** — 맨 위에 1 2 3 … n으로 인덱싱된 전체 데이터 막대가 있고, 그 아래 폴드1~폴드5 라벨이 붙은 5개 구간으로 나뉨. 1번~5번 행에서 주황(검증) 블록이 폴드1→폴드5로 한 칸씩 이동하고, 나머지 칸은 파랑(훈련). 범례: 파랑=훈련, 주황=검증.

### K-겹 교차검증 오류 계산 (p.123)

폴드 집합 $C_1,\cdots,C_K$, 각 폴드 크기 $n_k$

$$\mathrm{CV}(K) = \sum_{k=1}^{K} \frac{n_k}{n} MSE_k$$

- 여기서 $\mathrm{MSE}_k = \frac{1}{n_k}\sum_{i \in C_k}(y_i - \hat{y}_i)^2$ (K-폴드를 검증셋으로 두고 나머지로 적합한 예측 $\hat{y}_i$)
  - MSE(평균제곱오차): 예측이 얼마나 틀렸는지를 숫자로 표현한 값 (오차를 제곱해 평균)
- $K = n$이면 **Leave-One-Out 교차검증(LOOCV)**
  - 100개 문제(n)로 100개의 서로 다른 모의고사(K)를 만드는 것
- Leave-One-Out 교차검증에서 검증셋 크기는? **1**

### Leave-One-Out 교차검증 (p.124)

- **훈련셋(파랑)**: 관측치 하나만 제외한 나머지 전부
- **검증셋(주황)**: 제외한 1개 관측치
- 이 과정을 n번 반복해 나온 n개의 MSE 평균으로 테스트 오류를 추정

> **[그림3-2] Leave-One-Out** — 1번 행은 관측치 1만 주황(검증), 2번 행은 관측치 2, 3번 행은 관측치 3이 주황이고, 마지막 n번 행은 맨 끝 관측치 n이 주황. 나머지는 모두 파랑(훈련).

### LOOCV vs 10-겹 교차검증 (p.125-126)

- 자동차 데이터에서 **LOOCV**와 **10-겹 CV** 결과 비교(다항 차수에 따른 MSE 곡선)
  - 99문제 학습 후 1문제로 평가하기 100번 반복 VS 90문제 학습 후 10문제로 평가하기 10번 반복
- 두 방법의 **경향**과 **최적 차수**가 비슷
- LOOCV는 모든 데이터를 검증에 사용. 하지만 연산량이 많아서 학습 시간이 오래 걸림
- LOOCV가 10-겹 CV와 비교해 큰 성능 향상이 없으므로, 적절한 K를 선택하여 K-겹 교차검증을 사용하는게 좋음

> **[그림3-3] LOOCV vs 10-Fold CV** — 좌측 "LOOCV" 패널(주석 "1문제짜리 모의고사 100개")과 우측 "10-fold CV" 패널(주석 "10문제짜리 모의고사 10개"). 두 패널 모두 x축 Degree of Polynomial(2, 4, 6, 8, 10), y축 Mean Squared Error(16~28). 차수 1에서 24 부근이던 MSE가 차수 2에서 약 19로 급감한 뒤 평평하게 유지되는 모양이 두 패널에서 동일. 10-fold CV 패널은 반복 분할마다 여러 색 곡선이 겹쳐 그려져 있음.
>
> **[그림3-4] 참/추정 테스트 MSE (p.126)** — 세 개의 패널, 모두 x축 Flexibility(2, 5, 10, 20 로그 눈금), y축 Mean Squared Error. 각 패널에서 파랑(참 test MSE), 검은 점선(LOOCV), 주황(10-겹 CV) 곡선이 그려지고 각 곡선의 최소점이 X 표시로 찍혀 있음. 세 곡선의 모양과 최소점 위치가 서로 비슷하게 겹쳐 있어 CV 추정이 참 테스트 MSE를 잘 따라감을 보여줌. → 10-겹 CV 추정은 테스트 성능 추정의 좋은 대안.

## 확인문제 (3차시)

### 문제 1. 훈련 오류와 테스트 오류 (p.127-128)

훈련 오류와 테스트 오류에 대한 설명으로 옳은 것은?

① 훈련 오류는 새로운 데이터에 대한 예측 정확도를 측정한다.
② 테스트 오류는 학습에 사용된 데이터에 대해 계산한 평균 오차이다.
③ 궁극적으로 테스트 오류를 최소화하는 것이 학습의 목적이다.
④ 일반적으로 훈련 오류는 테스트 오류보다 크게 나타난다.
⑤ 훈련 오류는 항상 0에 수렴해야 좋은 모델이다.

**정답: ③**
해설: 학습의 목적은 일반화 성능을 높이는 것이며, 이는 테스트 오류를 최소화하는 것이다.
① 새로운 데이터는 테스트 오류가 담당한다. ② 이는 훈련 오류에 대한 설명이다. ④ 일반적으로 훈련 오류 < 테스트 오류이다. ⑤ 훈련 오류가 0에 가까우면 오버피팅일 가능성이 높다.

### 문제 2. 언더피팅과 오버피팅 (p.129-130)

다음 중 언더피팅과 오버피팅의 특징을 올바르게 설명한 것은?

① 언더피팅은 훈련 오류와 테스트 오류 모두 크게 나타난다.
② 언더피팅은 모델이 지나치게 복잡해 데이터 잡음까지 학습하는 현상이다.
③ 오버피팅은 모델이 단순해 중요한 패턴을 놓치는 현상이다.
④ 오버피팅은 훈련 오류가 크고 테스트 오류가 작게 나타난다.
⑤ 두 경우 모두 모델 복잡도를 높이면 해결된다.

**정답: ①**
해설: 언더피팅은 모델이 너무 단순하여 데이터 패턴을 제대로 학습하지 못해 훈련 오류와 테스트 오류 모두 크게 나타난다.
② 오버피팅에 대한 설명이다. ③ 언더피팅이 아니라 오버피팅의 반대 현상이다. ④ 오버피팅에서는 훈련 오류 ↓, 테스트 오류 ↑ 이다. ⑤ 언더피팅은 복잡도를 높이면 개선 가능하지만, 오버피팅은 오히려 더 심해진다.

### 문제 3. K-겹 교차검증 (p.131-132)

K-겹 교차검증(K-fold CV)에 대한 설명으로 옳지 않은 것은?

① 데이터를 K개 그룹(폴드)으로 나누어, 각 그룹을 한 번씩 검증셋으로 사용한다.
② K번 반복한 후 각 폴드별 평균 오차를 계산해 일반화 성능을 추정한다.
③ K=n일 경우 Leave-One-Out 교차검증(LOOCV)이다.
④ 교차검증은 데이터 활용성을 높이면서 테스트 오류에 대한 추정이 가능하다.
⑤ 교차검증시, 각 폴드에 속한 데이터가 겹쳐도 무방하다.

**정답: ⑤**
해설: 교차검증에서는 각 폴드별 데이터가 겹치지 않도록 명확히 분리해야 한다.

## 강의 정리 (3차시) (p.133-134)

**테스트 성능 평가**
- 훈련 오류: 학습에 사용된 데이터에서의 오류
- 테스트 오류: 새 데이터에서의 오류 → 일반화 성능 평가 기준
- 목표: 테스트 오류가 최소가 되는 모델 선택

**오버피팅 vs 언더피팅**
- 오버피팅: 모델이 지나치게 복잡 → 훈련 성능↑, 테스트 성능↓
- 언더피팅: 모델이 지나치게 단순 → 중요한 패턴을 놓쳐 오류 큼

**검증(Validation)**
- 데이터를 훈련셋/검증셋으로 분리
- 훈련셋으로 모델 적합, 검증셋으로 성능 측정
- Hold-out, K겹 교차검증(Cross-Validation) 활용

**K겹 교차검증**
- 데이터를 K개로 나눠 반복적으로 학습·평가
- 모든 데이터를 검증에 사용해 일반화 성능을 더 정확히 추정
- $K = n$: LOOCV (Leave-One-Out Cross Validation)

---

# 4차시 — AI & 기계학습 기초 4: 비지도 학습은 무엇인가?

## CONTENTS (p.136)

1. 비지도 학습
2. 클러스터링(Clustering)
3. K-means 클러스터링
4. 계층적 군집(Hierarchical Clustering)
5. 클러스터링 주의점

## 학습 목표 (p.137)

- 비지도 학습의 개념과 지도학습과의 차이점을 설명한다.
- 클러스터링의 개념과 대표적 방법론인 K-means와 계층별 군집법을 설명한다.

## 1. 비지도 학습

### 비지도학습이란? (p.139)

- **정의**: 레이블(정답) 없이 데이터의 구조·패턴·집단(잠재 서브그룹)을 찾아내는 학습
- **대표 과제**: 군집화(clustering), 차원축소(PCA 등), 밀도추정/이상치 탐지
- **출력**: "정답 예측"이 아니라 구조/요약/표현(embedding)

**핵심 질문**
- 무엇을 비슷함/다름으로 볼 것인가(거리·유사도 선택)
- 전처리(스케일 표준화 등)를 어떻게 할 것인가

> **[그림] 차원축소(PCA): 데이터 압축 및 시각화 과정** — 왼쪽은 "3D PCA space (PC1 vs PC2 vs PC3)" 3차원 산점도, 오른쪽은 "Flattened PCA space (PC1 vs PC2)" 2차원 산점도. 축 라벨은 PC1 (73.0%), PC2 (22.9%), PC3 (3.7%). 범례 Species: setosa(빨강), versicolor(주황), virginica(회색). 3차원 공간의 점들을 2차원 평면으로 눌러 압축·시각화하는 과정을 보여줌.
> 출처: https://www.linkedin.com/pulse/dimensionality-reduction-principal-component-analysis-binayak-xqiuc

### 비지도 vs 지도학습 (p.140)

- **지도학습**: 입력+라벨(정답)로 예측 모델 학습 — 예: 가격 예측, 악성 종양 예측
- **비지도학습**: 입력만으로 구조 학습 — 예: 고객 세그먼트
- **비지도학습 예 — 클러스터링**: 서로 비슷한 데이터끼리 묶어 동질 그룹 만들기 — 예: 비슷한 뉴스 분류(without label)

> **[그림1-1] 지도학습 vs 비지도학습** — b) Supervised learning 패널은 초록/빨강으로 라벨이 이미 색칠된 점들(축: Variable 1, Variable 2)이고, 아래 화살표를 따라가면 초록 영역과 빨강 영역을 나누는 결정경계가 그려진 그림이 됨. a) Unsupervised learning 패널은 모두 회색(라벨 없음) 점들이고, 아래 화살표를 따라가면 파랑·초록·주황·빨강·노랑 점선 원으로 묶인 5개 군집이 나타남.

## 2. 클러스터링(Clustering)

### 클러스터링이란? (p.142)

- **클러스터링**: 데이터 안에서 하위 집단(클러스터)을 찾는 기법들의 총칭
- **집단 내부**는 서로 **유사**, **집단 간**은 **상이**하도록 데이터를 분할
  - 집단 내부: 비슷한 성격끼리 묶이도록 만드는 것 — 예) 쇼핑몰에서 "비슷한 취향의 고객 그룹" 만들기
  - 집단 사이: 서로 다른 부류는 확실히 다르게 구분 — 예) 고가 제품만 사는 고객 vs 할인 제품만 사는 고객
- 유사/상이는 문제·도메인에 따라 달라짐
- 특성에 따라 클러스터가 잘 안 보이거나, 알고리즘마다 결과가 달라질 수 있음

### 예시: 마케팅 세그먼테이션 (p.143)

- 다수의 지표(가구 소득, 직업, 도심 거리 등)를 가진 많은 사람들에 대해 **특정 광고/상품**에 **더 반응할 하위집단**을 식별하고자 함
- **시장 세분화** 작업 자체가 **클러스터링**에 해당

> **[그림2-2] 마케팅 세그먼테이션** — 왼쪽은 여러 색이 뒤섞인 사람 아이콘 무리, 화살표 오른쪽은 같은 색끼리 모인 4개의 원(주황 그룹, 파랑 그룹, 빨강 그룹, 회색 그룹)으로 정리된 모습.

### 두 가지 대표 클러스터링 기법 (p.144)

- **K-평균(K-means)**: K(클러스터 수)를 **미리 정해** 분할
- **계층적 군집(Hierarchical)**: K를 사전에 고정하지 않음

> **[그림2-3] K-Means와 Hierarchical** — 왼쪽 "K-Means Clustering"은 x축 −3~3 범위 산점도에 파랑·초록·보라·노랑 4개 군집이 색으로 구분되고 각 군집 중심에 빨간 X가 찍혀 있음. 오른쪽 "Hierarchical Clustering Dendrogram"은 y축 0~50 이상의 병합 높이를 갖는 덴드로그램으로, 주황 가지와 분홍 가지가 위로 올라가며 하나로 합쳐지는 트리 구조.

## 3. K-means 클러스터링

### K값에 따른 결과 (p.146)

- 패널: **K=2, 3, 4**에서 각 K-means 결과(점 색상 = 할당된 클러스터)
- 클러스터 결과에서 특정 색상은 의미가 없고, 점들이 다른 색이라는 것은 서로 다른 클러스터에 속해있다는 의미

> **[그림3-1] 다른 K값에 대한 K-Means 결과** — 동일한 산점도 데이터에 대해 K=2(2색), K=3(3색), K=4(4색)로 각각 색 구분된 세 패널이 나란히 놓여 있음.

### K-means의 표기(군집 집합) (p.147-148)

- 관측치 인덱스 집합을 $C_1, \ldots, C_K$ 라 하면: $C_1 \cup \cdots \cup C_K = \{1, \ldots, n\}$ (각 관측치는 적어도 하나의 군집에 속함)
- 모든 다른 클러스터 $k \neq k'$ 에 대해, $C_k \cap C_{k'} = \emptyset$ → 하나의 데이터가 여러 그룹에 동시에 속할 수 없음
- $i$번째가 $k$번째 군집이면 $i \in C_k$ (비중첩 분할)
- 모든 데이터를 겹치지 않게($K$개 군집이 서로 겹치지 않음) 빠짐없이($K$개 군집 중 하나에는 반드시 포함) 정확히 하나의 군집에만 나눔

**핵심 아이디어**

- 좋은 군집화 = **클러스터 내부 변동**(Within-Cluster Variation)이 작은 분할
- 목표: **클러스터 거리·차이의 합**이 **최소**가 되도록 데이터를 나누는 것
  → '그룹 안에서 얼마나 모여 있는지'를 기준으로 K개의 그룹을 만드는 알고리즘

### K-means 알고리즘 (p.149-150)

1. **초기화**: 관측치들에 **무작위로 $1\cdots K$** 클러스터를 임시 부여
2. **반복**(할당이 더 이상 바뀌지 않을 때까지):
   - **2a.** 각 클러스터의 **중심(centroid)** 계산(특성 평균 벡터)
   - **2b.** 각 관측치를 **가장 가까운 중심**의 클러스터에 **재할당**(거리 예 = 유클리드)

**알고리즘 특성**

- 중심점과의 평균 거리가 계속 줄어들기 때문에 매 반복마다 결과는 더 좋아짐
- 하지만 최적 해를 항상 찾는 건 아님 → **초기값**에 따라 **지역 최솟값**으로 수렴 가능
  → 지역 최소값(Local minimum) 문제라고 부름

> **[그림3-2] K-Means 알고리즘** — 6개 패널. "Data"(모두 검은 점), "Step 1"(주황/초록/분홍으로 무작위 색 부여), "Iteration 1, Step 2a"(세 중심점이 거의 한 곳에 겹쳐 표시), "Iteration 1, Step 2b"(점들이 재할당되어 색 영역이 분리되기 시작), "Iteration 2, Step 2a"(중심점 3개가 각 군집으로 이동), "Final Results"(주황·분홍·초록 3개 군집과 각각의 중심점이 안정된 최종 결과).

### 초기값의 영향 (p.151)

- 서로 다른 초기 레이블에서 **최종 분할과 목표값**(패널 상단 숫자)이 달라짐
- **초기화의 중요성**: 여러 번 시도 권장

> **[그림3-3] 초기값의 영향** — 동일한 산점도 데이터에 대해 서로 다른 초기값으로 K-means를 6번 수행한 결과 패널. 각 패널 상단에 목표값이 표시되며 값은 320.9, 235.8, 235.8(윗줄), 235.8, 235.8, 310.9(아랫줄)로, 235.8인 4개 패널은 붉은색으로 강조되어 있고 320.9·310.9인 패널은 회색으로 표시됨. 235.8 패널들은 색 배치만 다를 뿐 동일한 분할이고, 320.9·310.9 패널은 클러스터 경계 자체가 다르게 잡힘.

## 4. 계층적 군집(Hierarchical Clustering)

### 개념 (p.153)

- K-means는 클러스터 수 **K를 미리 지정**해야 하는 단점이 존재함
- **계층적 군집**은 K를 **고정하지 않고** 전체 구조를 **덴드로그램**으로 제공
- 이번 강의에서는 상향식(agglomerative)을 다룸: **잎→몸통**으로 병합

### 덴드로그램에서 K개 군집 얻기 (p.154)

- 덴드로그램에서 수평선 높이(거리)를 기준으로 가위질하여 K개 군집을 얻음

> **[그림4-1] 덴드로그램** — 동일한 덴드로그램 3개를 나란히 놓고, 첫 번째는 자르지 않은 상태, 두 번째는 높이 약 9 부근에서 점선+가위로 잘라 2개 군집(분홍·초록), 세 번째는 높이 약 5 부근에서 잘라 3개 군집(분홍·노랑·초록)이 되는 과정을 보여줌. 오른쪽에는 세로축 구조를 위에서부터 **몸통(root)**, **가지(branch)**, **잎(leaf) — 실제 데이터들**로 표시.

### 계층적 군집 알고리즘(상향식) (p.155)

1. n개 관측에 대해 **쌍별 비유사도** 계산, **각 관측치를 하나의 클러스터**로 시작
2. i개 클러스터 간 **쌍별 비유사도**를 모두 계산해 **가장 유사한** 두 클러스터를 **병합**
3. 병합 후 남은 **i-1개 클러스터** 사이의 **새 비유사도**를 다시 계산
   - 2~3단계를 **i=n, n-1, …, 2에 대해 반복**

> **[그림4-2] 계층적 군집 알고리즘(상향식)** — 왼쪽은 9개 관측치(1~9)의 덴드로그램(세로축 0.0~3.0), 오른쪽은 같은 9개 점의 $X_1$–$X_2$ 산점도(가로축 −1.5~1.0, 세로축 −1.5~0.5).

### 병합 진행 예와 계산량 (p.156-157)

- 데이터들이 **점차 큰 클러스터로 합쳐지는 과정**
- 매 단계에서 클러스터들끼리의 병합이 이루어짐
- 1개의 단일 클러스터가 될 때까지 진행
- **계층적 군집의 계산량**: 매 단계에서 모든 클러스터 쌍 간의 거리를 계산해야 함. 데이터의 수가 많은 경우 K-means에 비하여 계산량이 많음

> **[그림4-3] 계층적 군집 병합 진행 예** — $X_1$–$X_2$ 산점도 4개 패널. 처음에는 9개 점이 모두 개별 상태이고, 다음 패널에서 5와 7이 빨간 박스로 묶이고, 이어서 1과 6이 보라 박스로 묶이며, 마지막 패널에서 8이 {5,7}과 초록 박스로 묶여 점차 큰 클러스터가 형성됨.

### 링크(link)의 유형 (p.158-159)

- **Single(최소 거리) 링크**: 두 개 클러스터 내 데이터 **쌍별 거리 중 최소값**을 군집 간 거리로
- **Complete(최대 거리) 링크**: 두 개 클러스터 내 데이터 **쌍별 거리 중 최대값**을 군집 간 거리로
- **Average(평균 거리) 링크**: 두 개 클러스터 내 데이터 **쌍별 거리의 평균**을 군집 간 거리로

같은 데이터라도 **링크 선택**에 따라 **클러스터링 결과**(덴드로그램)가 달라질 수 있음. 하나의 링크만 시도하는 것이 아니라 다른 종류의 링크도 사용 권장.

> **[그림4-4] 링크의 유형** — 노란 점 무리와 파란 점 무리 사이를 빨간 선으로 잇는 그림 3개. single-link는 두 무리에서 서로 가장 가까운 점끼리 짧게 연결, complete-link는 가장 먼 점끼리 길게 연결, average-link는 무리 중앙부를 잇는 중간 길이 선으로 표현됨.
>
> **[그림4-5] 링크에 따른 덴드로그램 변화** — 동일 데이터에 대한 Average Linkage(주황), Complete Linkage(하늘색), Single Linkage(초록) 덴드로그램 3개를 비교. Average와 Complete는 비교적 균형 잡힌 트리 모양이나 Single Linkage는 한쪽으로 길게 이어지는 사슬 형태로 모양이 확연히 다름.

## 5. 클러스터링 주의점 — 체크리스트 (p.161)

- **스케일링**: 표준화(평균 0, 표준편차 1로 입력 변수 변환)가 필요한가? → Yes, **변수 단위 차이 영향**이 큼
- **몇 개의 클러스터**가 적합한지? → K-means·계층적 모두 어려운 문제, 합의된 정답 없음
- 단일 시도가 아닌 여러 번 시도 권장됨

## 확인문제 (4차시)

### 문제 1. 비지도 학습의 예 (p.162-163)

다음 중 비지도 학습의 예로 해당하는 것은?

- A. 스팸/이메일 분류
- B. 집값 변화 예측
- C. (라벨이 없는 데이터에서) 구매 성향별 고객 묶음
- D. 주간 날씨 예측

**정답: C. 구매 성향별 고객 묶음**
해설: 정답이 없는 묶음 문제로 전형적인 클러스터링 문제

### 문제 2. K-means vs 계층적 군집 (p.164-165)

다음은 클러스터링 기법에 대한 설명이다. 각 설명이 K-means의 특징인지, 계층적 군집의 특징인지 알맞게 분류하시오. ※ 단, 두 방법 모두에 해당하지 않을 수 있다.

- A. 클러스터 수 K를 미리 지정해야 한다.
- B. 덴드로그램(Dendrogram)을 통해 전체 구조를 시각적으로 제공한다.
- C. 데이터 간 병합(상향식) 과정을 통해 클러스터를 형성한다.
- D. 클러스터 내부 변동(Within-cluster variation)의 합이 최소가 되도록 분할한다.
- E. 매우 큰 데이터셋에도 계산 복잡도가 크게 늘지 않아 효율적이다.

**정답: K-means: A, D, E / 계층적 군집: B, C**

해설: K-means는 데이터를 K개의 그룹으로 나누는 대표적 기법으로, 군집 내 분산을 최소화하도록 반복 계산한다. 빠르고 대규모 데이터에 효율적이지만 클러스터 수를 미리 지정해야 하고 복잡한 모양의 데이터에는 한계가 있다. 계층적 군집은 데이터를 병합·분할하여 트리(덴드로그램) 구조를 만드는 방식으로, 클러스터 수를 미리 정하지 않아도 되고 데이터 구조를 시각적으로 파악하기 쉽다. 그러나 데이터가 많을수록 계산 비용이 커져 대규모 데이터에는 비효율적이다.

## 강의 정리 (4차시) (p.166-167)

**비지도 학습**
- 레이블(정답)이 없는 데이터를 통해 데이터의 구조·패턴·집단을 학습하는 방법
- 데이터 탐색, 군집 분석, 시각화, 차원 축소, 이상치 탐지 등

**클러스터링(Clustering)**
- K-평균(K-means): K(클러스터 수)를 미리 정해 분할
- 계층적 군집(Hierarchical): K를 사전에 고정하지 않음

**링크(link) 유형**
- Single(최소 거리) 링크: 데이터 쌍별 거리 중 최소값을 군집 간 거리
- Complete(최대 거리) 링크: 데이터 쌍별 거리 중 최대값을 군집 간 거리
- Average(평균 거리) 링크: 데이터 쌍별 거리의 평균을 군집 간 거리

---

# 부록

## 참고 자료 목록 (p.170-174)

| No. | 차시 | 목차 | 내용 |
|---|---|---|---|
| 1 | 1차시 | 1. AI, ML, DL의 정의 | AI·ML·딥러닝의 개념 차이와 포함 관계를 개미/거미 분류 예시로 설명 |
| 2 | 1차시 | 2-1. 데이터 구성 요소 | 피처(Feature), 레이블(Label), 모델(Model)이 무엇인지를 집값 예측·스팸 분류 등 실생활 예시로 설명 |
| 3 | 1차시 | 2-1. 데이터 구성 요소 | ML에서 '러닝'의 의미와 Feature, label, weight, bias를 수식으로 설명 |
| 4 | 1차시 | 3. 단일 피쳐 기반 학습 | 통계적 학습의 핵심 개념인 모델 추정 방법(파라메트릭/비파라메트릭), Bias-Variance 트레이드오프, MSE, 분류 vs 회귀, KNN을 수식과 예시로 설명 |
| 5 | 1차시 | 2-1. 데이터 구성 요소 | 지도 학습의 핵심 개념인 데이터(특성·라벨), 모델, 학습, 평가, 추론의 흐름을 단계별로 설명한 Google 공식 입문 문서 |
| 6 | 2차시 | 1. 지도학습의 개념 | ML의 정의, 지도학습과 비지도학습의 차이를 개·고양이 예시로 설명 |
| 7 | 2차시 | 2. 회귀(Regression) | 선형 회귀에서 사용하는 최소제곱법과 로지스틱 회귀에서 사용하는 최대우도법의 개념과 차이를 수식으로 설명 |
| 8 | 2차시 | 2. 회귀(Regression) | 결정계수($R^2$)의 개념과 수식, 해석 방법, 독립변수가 많을 때 사용하는 조정된 결정계수(Adjusted $R^2$)까지 설명 |
| 9 | 2차시 | 3. 분류(Classification) | 혼동 행렬의 TP·FP·TN·FN 개념과 이를 기반으로 한 Accuracy, Precision, Recall, F1 Score를 스팸 분류 예시로 설명 |
| 10 | 2차시 | 4. 학습의 목적 | 과적합(Overfitting)의 개념, 과소적합과의 차이, 모델 검증·정규화·데이터 증대 등으로 과적합을 방지하는 방법을 설명 |
| 11 | 3차시 | 1. 테스트 성능 평가 | 용존산소 농도 실시간 예측을 주제로 한 논문을 통해, Bias-Variance Tradeoff 개념과 과적합 없이 일반화 성능이 높은 모델을 선택하는 기준을 이해 |
| 12 | 3차시 | 2. 검증셋(Validation Set) 접근 | 교차 검증(Validation Set, LOOCV, k-Fold)과 부트스트랩(Bootstrap)을 포함한 리샘플링 기법의 개념과 모델 성능 평가 및 오차 추정에 활용하는 방법을 설명 |
| 13 | 3차시 | 2. 검증셋(Validation Set) 접근 | Train, Validation, Test 세 데이터셋의 역할과 차이, 그리고 K-fold 교차검증의 개념을 코로나 증상 데이터 예시로 설명 |
| 14 | 3차시 | 3. K-겹 교차검증 | 예측 오차를 추정하고 모델 복잡성을 파악하는 데 사용되는 K-겹 교차 검증 기법을 영상으로 설명 |
| 15 | 4차시 | 1. 비지도 학습 | K-means, DBSCAN, 계층적 군집분석의 개념·군집화 과정·장단점을 유사도(거리) 측정 방법과 함께 설명 |
| 16 | 4차시 | 1. 비지도 학습 | 스마트 제조 환경에서 머신러닝과 딥러닝의 개념, 차이점, 산업 현장 적용 가능성을 소개하는 KSA 칼럼 |
| 17 | 4차시 | 1. 비지도 학습 | 딥러닝 텐서플로 교과서의 비지도 학습 챕터로, PCA·K-평균 군집화·밀도 기반 군집 분석(DBSCAN) 등 주요 비지도 학습 알고리즘을 학습 |
| 18 | 4차시 | 2. 클러스터링(Clustering) | 군집화 설명 및 그 중 가장 많이 사용되는 K-평균 군집화 알고리즘의 절차를 소개 |
| 19 | 4차시 | 3. K-means 클러스터링 | K-평균 군집화의 개념, 중심점 선택 → 클러스터 할당 → 새로운 중심점 계산 → 수렴 확인의 4단계 학습 과정을 설명 |
| 20 | 4차시 | 4. 계층적 군집 | 계층적 군집화의 개념, 덴드로그램을 활용한 군집화 과정, 다양한 거리 측정 방법에 대해 설명하며 계층적 군집화의 장점을 소개 |

(각 항목의 URL은 원본 슬라이드에 QR 코드로 제공됨)

## 실습 미리보기: 데이터 EDA 및 모델 학습 (p.176-179)

**학습 주제**: 데이터 구성요소와 학습 정의를 기반으로 지도·비지도 학습을 이해하고, 분류모델 학습·검증 및 K-Means 클러스터링을 실습해보자.

- 탐색적 데이터 분석(EDA)을 통해 데이터 특성과 분포를 이해할 수 있어요 [1차시 2-1 참고] — 탐색적 데이터 분석(EDA), StandardScaler
- 머신러닝 전처리 및 분류 모델 학습·평가를 통해 지도학습을 이해할 수 있어요 [2차시 1-3 분류, 3차시 1-1 테스트 성능 평가 참고] — Logistic Regression, ROC-AUC
- 차원 축소(PCA) 및 KMeans 군집화를 통해 비지도 학습을 이해할 수 있어요 [4차시 1-2 클러스터링, 1-3 K-means 참고] — 차원 축소(PCA)

### 탐색적 데이터 분석(EDA)과 StandardScaler (p.177)

- **탐색적 데이터 분석(EDA)**: 데이터의 특징과 구조를 직관적으로 이해하기 위해 데이터 분포, 중앙값·사분위수·상관관계 등을 시각화하여 탐색하는 기법
  - 기초 통계치(평균, 중앙값 등), 결측치/이상치 탐지, 상관계수 히트맵 등
- **StandardScaler**: 차이가 큰 피처로 인해 특정 피처에 치우치지 않도록, 피처별 평균을 0, 분산을 1로 표준화하여 머신러닝 모델 학습을 안정화하는 전처리 기법

> **[그림1-1] EDA 상관계수 히트맵** — "Correlation between features" 제목의 하삼각 히트맵. 축 항목은 alcohol, malic_acid, ash, alcalinity_of_ash, magnesium, total_phenols, flavanoids, nonflavanoid_phenols, proanthocyanins, color_intensity, hue, od280/od315_of_diluted_wines, proline, quality이며 컬러바는 −0.8~0.8. 예: total_phenols–flavanoids 0.86, quality–flavanoids −0.85, od280/od315_of_diluted_wines–flavanoids 0.79 등 값이 셀마다 표기됨.

### Logistic Regression과 ROC-AUC (p.178)

- **Logistic Regression**: 선형 방정식을 기반으로 한 회귀 계수 해석을 통해 확률적 분류를 수행하는 지도 학습 알고리즘
- **ROC (Receiver Operating Characteristic Curve)**: 임계값(threshold)을 변화시켰을 때, TPR과 FPR의 관계를 나타낸 곡선
- **AUC (Area Under the Curve)**: ROC 곡선 아래 면적
- **TPR**: 진짜를 진짜로 잡는 비율 / **FPR**: 가짜를 진짜로 잡는 비율

> **[그림1-2] ROC-AUC** — 가로축 FPR, 세로축 TPR 평면에 좌하단에서 우상단으로 볼록하게 올라가는 초록색 ROC 곡선이 그려지고, 대각선 점선 위쪽 곡선 아래 회색 음영 영역이 AUC로 표시됨.

### 차원 축소(PCA) (p.179)

- 고차원 데이터를 주성분으로 변환하여 정보를 최대한 보존하면서 차원을 축소하는 기법
- 차원 축소 후 2차원이나 3차원으로 쉽게 시각화할 수 있어, 데이터의 분포나 구조를 직관적으로 파악 가능

> **[그림1-3] 차원 축소(PCA)** — 3개 칸에 각각 3차원 정육면체(라벨 3), 2차원 정사각형(라벨 2), 1차원 선분(라벨 1)이 그려져 있고 각 도형의 꼭짓점에 빨강·파랑 점이 찍혀 있어 차원이 3→2→1로 줄어드는 과정을 보여줌.
